# 公众号抓取层根因修复报告

**日期**：2026-07-31 21:50
**触发**：用户授权「你自己判断 + 直接推送」，自主处置两个长期积压问题
**结论**：连续 6 轮（#74–#79）被记为「RSS 抓取质量问题待查」的悬案已定位并根治

---

## 一、根因：不是抓不到，是被限流后把失败当成了空文章

付费 RSS 服务 `wechatrss.waytomaster.com/api/article` **存在服务端防风控限流**——这一点与代码里的旧注释「单篇接口稳定、无限流」完全相反，正是该错误注释误导了此前 5 轮排查方向。

实测服务端在限流时会明确回传原因：

| 服务端返回 | 性质 | 应对 |
|---|---|---|
| `请求过于频繁，请 N 秒后重试` | 可恢复 | 按 N 退避后重试 |
| `文章获取过快，请1秒后重试（防风控）` | 可恢复 | 退避重试 |
| `触发微信安全验证...` | 需等待 | 不重试，跳过 |

**故障链**：

```
限流失败 → fetch_article_content() 的 except: return ""  ← 静默吞错
        → sync_wx_articles 把空正文照常落盘
        → 该 url 进入 _existing_urls() 去重集合
        → 下轮增量跳过 → 正文永久丢失，无自愈可能
```

**实际损失**：全库 641 篇中 **592 篇是空壳（92%）**，远超此前目测的 70–73%。LLM 精读环节长期在「读空气」，`actionable=0` 的静默轮次多数属误伤而非真无观点。

---

## 二、修复清单

| # | 文件 | 改动 |
|---|---|---|
| 1 | `.workbuddy/scripts/wx_rss_auth.py` | 新增 `fetch_article_content_ex() -> (content, err)`，正则解析「N秒后重试」做退避重试（max_retry=4，单篇上限 40s），遇安全验证立即返回；旧函数保留为兼容包装；补 `import re` |
| 2 | `.workbuddy/scripts/sync_wx_articles.py` | **抓取失败不落盘**（留待下轮重抓）；请求间隔 `GAP_SEC` 0.3s → 1.5s；结尾输出失败明细 |
| 3 | `.workbuddy/scripts/backfill_wx_content.py` | **新建**，回填历史空壳；幂等可重跑；只改 `content_text/content_len/backfill_time`，不动其他字段；连撞 3 次安全验证自动收工 |
| 4 | `.workbuddy/scripts/read_wx_articles.py` | 新增 `file_key() = md5(filename)[:12]` 作 processed 主键，根治 #38 起的文件名换行符 bug；`load_processed` 兼容旧格式；`article_id` 复用同函数避免双套 ID |

**数据迁移**：`read_wx_articles_processed.txt` 554 行（全旧格式）→ 488 个去重 hash key，已备份为 `.bak-20260731`。

---

## 三、验证闭环

1. **回填**：最新 60 篇中处理 11 篇 → 成功 8 篇（失败 3 篇为逐篇安全验证，属正常保护）
2. **重跑精读** 8 篇 → `actionable=1` / **`signals_new=3`**

新落信号（来自【飞龙山侠】偏多·置信高，市场反转/交换机方向）：

| 标的 | 代码 | 依据 | 合规 |
|---|---|---|---|
| 共进股份 | 603118 | 交换机龙头，反包走强带动板块 | 沪主板 ✅ |
| 紫光股份 | 000938 | 换手核心，上板汇聚合力 | 深主板 ✅ |
| 星网锐捷 | 002396 | 趋势新高，强度领先 | 中小板 ✅ |

三只均非创业板/科创板/北交所/ST，约束校验通过。与 21:00 那轮【草原猎手景月】提到的「紫光/星网」形成交叉印证——那轮因只有简称无代码未落池，本轮补齐。

---

## 四、后续安排

新建自动化 `1785506323216`「🔧【知识库】公众号正文回填(限流友好)」：

- 频率 `FREQ=HOURLY;INTERVAL=2`，每轮 40 篇、间隔 2.0s
- 剩余约 536 篇，预计 1–2 天补完
- 仅在 `remaining == 0` 时推送完成通知，日常静默

**观察指标**：后续轮次空正文率应持续下降，`actionable` 命中率应显著提升。若空正文率仍居高不下，需检查限流参数是否需进一步放宽。

---

## 五、飞书推送

- 根因修复报告：`om_x100b69feec5d6ca0b12c8f92f92d6c8`
- 补充轮公众号精华：`om_x100b69feed48cca0b1573dfed7a0e69`

---

*本报告仅供参考，不构成个人投资建议。*
