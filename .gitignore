# Python
__pycache__/
*.py[cod]
*.pyo
*.so
.venv/

# 环境变量
.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# 系统
.DS_Store
Thumbs.db

# 测试/覆盖率
.coverage
.pytest_cache/
htmlcov/

# 日志
*.log
logs/*.log

# WorkBuddy 本地数据 (不提交)
.workbuddy/logs/
.workbuddy/.coverage
.workbuddy/.workbuddy/
.workbuddy/workbuddy.db
# Core data files (tracked) — whitelist only these, ignore rest
.workbuddy/data/*
!.workbuddy/data/automation_health.json
!.workbuddy/data/macro_data.json
!.workbuddy/data/stock_pool.json
!.workbuddy/data/simulation/
!.workbuddy/data/simulation/portfolio.json
!.workbuddy/data/sector_cache.json
!.workbuddy/data/user/

# L2 Skills 下沉：项目内仅为指向用户级 ~/.workbuddy/skills/ 的本地 symlink
# （绝对路径，clone 后断链，非项目资产，不提交）。templates/ 为项目实体模板，保留。
.workbuddy/skills/*
!.workbuddy/skills/templates/
!.workbuddy/data/user/portfolio.json
# Runtime-only data files under .workbuddy/data/ (explicitly ignored)
.workbuddy/data/.backup*/
.workbuddy/data/.backups/
.workbuddy/data/cache/
.workbuddy/data/golden_results/
.workbuddy/data/*.db
.workbuddy/data/*.lock
.workbuddy/data/alert_state.json
.workbuddy/data/midday_data.json
.workbuddy/data/next_week_data.json
.workbuddy/data/pool_health.json
.workbuddy/data/qts_signals.json
.workbuddy/data/risk_snapshot.json
.workbuddy/data/signal_history.json
.workbuddy/data/signal_verified.json
.workbuddy/data/signal_verify_*.json
.workbuddy/data/signal_weights.json
.workbuddy/data/subscribe_candidates.json
.workbuddy/data/sysinfo.json
.workbuddy/data/fish_basin_sectors_*.json
.workbuddy/data/article_stock_discovery.json
.workbuddy/data/evening_report_data.json
.workbuddy/data/macro_weekly.md
.workbuddy/data/portfolio.json
.workbuddy/data/portfolio.json.lock
.workbuddy/data/claw-code-audit.md
.workbuddy/data/simulation/.backups/
.workbuddy/data/simulation/backups/
.workbuddy/data/simulation/decision_log*/
.workbuddy/data/simulation/trading_memory.*
.workbuddy/data/simulation/_orphan_*
.workbuddy/data/simulation/*.bak-*
.workbuddy/data/simulation/*.migrated*
.workbuddy/data/simulation/experiments/
.workbuddy/data/user/*.bak-*
# ⚠️ 存在两套 automations 记忆目录，本行只覆盖 A（2026-08-31 run#39 实锤）：
#   A) .workbuddy/automations/*/memory.md          ← 历史路径；磁盘 77 个，
#      其中 22 个已 tracked（gitignore 对已跟踪文件无效，本行管不到它们）
#   B) .workbuddy/memory/automations/*/memory.md   ← 平台当前实际写入路径；
#      磁盘 38 个，0 个 tracked，且无任何规则命中 → 在 git status 中可见
# 刻意不给 B 加忽略规则：加了会从「可见的未跟踪」变成「完全隐形」，
# 盲区只会更深。是否让 B 入库（56.7KB）属设计决策，勿由巡检自动化擅自定。
# 另注：git status 默认折叠未跟踪目录，B 的 38 个文件只显示为 1 行
# 「?? .workbuddy/memory/automations/」，须用 --untracked-files=all 才能看清。
.workbuddy/automations/*/memory.md
.workbuddy/archive/
archive/
.workbuddy/experiments/
.workbuddy/reports/
.workbuddy/artifacts/
.workbuddy/knowledge/
.workbuddy/memory/.backups/
.workbuddy/automations/*/.backups/

# 密钥（本地使用，不上传 GitHub）
scripts/secrets.py
scripts/legacy_secrets.py
scripts/.prompt_cache_config.json

# 运行时状态/一次性产物 (不提交, 防仓库膨胀与无意义 churn)
.workbuddy/data/article_signals.json
.workbuddy/scripts/.ops_alerted.json
.workbuddy/scripts/.watchdog_alerted.json
.workbuddy/scripts/.runbook_fuse.json
.workbuddy/scripts/*refresh_close_*.py
.workbuddy/scripts/bt_kdj_scan_result.json

# 临时
tmp/
temp/
output/

# 飞书认证二维码（自动生成，无需跟踪）
lark-auth-qr*.png

# 自动生成的 git 状态日志
.workbuddy/memory/git_status_log_*.md
.workbuddy/memory/2026-*-*.md
.workbuddy/automations/*/dedup_*.json
.workbuddy/scripts/unified_self_heal_log.json
# 运行时自动生成的自动化目录（非源码，仅运行态）
.workbuddy/automations/automation-1785123941471/
.benchmarks/

# 运行时数据 (不提交)
/data/
/reports/
claw.egg-info/
.uv/
*.egg-info/

# 外部工具配置 (本地)
.agents/
.brv/
.claude/

# 运行时输出 (不提交)
src/claw/strategies/data/
src/claw/strategies/reports/

# 备份文件
*.bak.*
*.bak-*

# Stray 文件防回归（源自两日变更审计）
$DB
.workbuddy/data/alpha/*
!.workbuddy/data/alpha/alpha_factors_kept.json

# 死副本：经验库权威位置为根 .learnings/，.workbuddy/.learnings 不再跟踪
.workbuddy/.learnings/
