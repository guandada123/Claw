# ============================================================
# Claw — Security Scan (Unified Standard)
# Runs: weekly schedule + on dep/code change + manual
# ============================================================
name: Security Scan

on:
  schedule:
    - cron: "0 8 * * 1"  # 每周一 8:00 UTC
  push:
    branches: [main]
    paths:
      - "pyproject.toml"
      - "**/*.py"
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.12"

jobs:
  safety-check:
    name: Dependency Scan (safety)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install deps + safety
        run: |
          pip install safety
      - name: Safety check
        run: safety check --full-report --output json > safety-report.json || true
        continue-on-error: true
      - name: Upload safety report
        uses: actions/upload-artifact@v7
        with:
          name: claw-safety-report
          path: safety-report.json
      - name: Summary
        run: |
          VULN_COUNT=$(python3 -c "
          import json
          with open('safety-report.json') as f:
              data = json.load(f)
          vulns = data.get('vulnerabilities', [])
          print(len(vulns))
          ") || echo "0"
          echo "## Safety Check" >> $GITHUB_STEP_SUMMARY
          echo "Vulnerabilities found: **${VULN_COUNT:-0}**" >> $GITHUB_STEP_SUMMARY

  bandit:
    name: Code Scan (bandit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install bandit
        run: pip install bandit[toml]
      - name: Bandit scan
        run: |
          bandit -r .workbuddy/scripts/ .workbuddy/lib/ -f json -o bandit-report.json \
            -ll --skip B101,B324,B310,B108,B306 || true
        continue-on-error: true
      - name: Upload bandit report
        uses: actions/upload-artifact@v7
        with:
          name: claw-bandit-report
          path: bandit-report.json
      - name: Summary
        run: |
          COUNT=$(python3 -c "
          import json
          with open('bandit-report.json') as f:
              data = json.load(f)
          print(len(data.get('results', [])))
          ") || echo "0"
          echo "## Bandit Code Scan" >> $GITHUB_STEP_SUMMARY
          echo "Issues found: **${COUNT:-0}**" >> $GITHUB_STEP_SUMMARY

  secret-scan:
    name: Secret Detection (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v3
        # 使用 gitleaks 默认配置
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
