# 2026-07-18 工作日志

## 系统维护（automation-1780769419635, 每日 3:30）
- 深度: daily（周六，非周日/非1号）→ 仅 PHASE1 只读检查
- 用户级记忆: SOUL 2468B(80%⚠️) / MEMORY **2857B(🔴>2KB软阈值，跨项目文件，未擅改)** / USER 914B(89%⚠️) / IDENTITY 400B(✅) — 与7-17持平，无新增异常
- 项目记忆: MEMORY.md 健康；日日志 8（补建今日）；自动化 memory 无>3000B ✅
- 日志连续性: 仅缺今日（本次补建）；7天窗口内其余齐全
- 修复: 无（daily只读）；**未推送飞书群**（维持7-15/7-16/7-17决议：系统维护类铁律优先，冲突待用户裁）
- 待裁: 用户级 MEMORY.md 已🔴超2KB，建议下次周日维护时蒸馏瘦身（跨项目文件，需用户授权再动）

## 成本监控（automation-1782002819199, v9 08:58 s1槽）
- 精简告警模式（HOUR=08, IS_DAILY=0）。全绿 → [SILENT] 铁律优先不推送。
- 今日 ¥3.3650(4次) | 本月 ¥199.84/¥400(228次)，剩余¥200.16，预估月底¥333 ✅ 预算内
- 单小时<¥20 / 今日<¥25 / 本月<¥400 / 模型占比33.6%(<50%) 全未触发
- 6h槽位锁 s1 标记成功（recheck=1），log_estimate 记¥0.135 成功

## 知识库挖掘 v8 三项决策落地（09:54）
- 用户拍板: #1 阈值走A / #2 新号走本地订阅 / #4 按我计划去重（#3 mtime重构留待后续）
- `mine_wx_articles_v8.py`: QUALITY_WIN_RATE 40→25（熊市期恢复信号流；现选3优质号）
- `discover_gzh_accounts.py`: 删 Pinchtab 全文抓取，改「红狐→searchbiz解析fakeid→POST /api/rss/subscribe 接入 localhost:5001 本地订阅」；真值源=本地订阅列表；仅 hit_rate≥40% 自动订阅，其余写 subscribe_candidates.json(pending)；修解析兜底误订阅bug
- `merge_signal_ranking.py`: 按 name 跨源去重，红狐与RSS重名号不重复计入/不误推接入；验证 deduped=4 无重复名
- 实测误建 test 垃圾订阅已清理(剩15真实)；discover 端到端跑通(4候选全 already_subscribed)

## 知识库挖掘 v8 — 全开订阅（10:04）
- 用户指令「全开」→ `discover_gzh_accounts.py` 新增总开关 `_AUTO_SUBSCRIBE = True`，订阅判定改为：`fakeid 且 (AUTO_SUBSCRIBE 或 hit_rate≥40%)` → 自动订阅，否则 pending
- 验证: 语法OK；布尔逻辑确认任意 hit_rate(含0/None)在 AUTO_SUBSCRIBE=True 下均判定可订阅；端到端跑通(123号/4候选/全 already_subscribed→0新增0垃圾)
- 效果: 下周六发现未订阅新候选号将无视 hit_rate 直接接入本地订阅(候选须含股票提及已兜底防灌水)

## 知识库挖掘三处技术债修复（11:5x, 用户「ABC全做」）
- A. mtime 增量闸→文件名清单（`.workbuddy/scripts/mine_wx_articles_v8.py`）：删 `get_baseline/set_baseline` + `BASELINE_FILE`；新增 `load_processed/save_processed` + `PROCESSED_FILE`。增量闸改为 `f.name not in processed_set`，根治晚到旧 mtime 文件永久漏捕。
- B. 胜率→近N日滚动（`src/claw/monitoring/signal_verify.py` build_report）：`ROLLING_DAYS=30`/`MIN_ROLLING_SAMPLES=10`，窗口样本不足回退累计。新增透明字段 win_rate_basis/rolling_window_days。
- C. 全开订阅上限（`scripts/discover_gzh_accounts.py`）：`_AUTO_SUBSCRIBE_CAP=40`，达上限降级 pending_cap。
- 验证：py_compile + 单测全绿。提交 `5967b25`（仅 A/B/C 三文件）。
- 清理：旧 `wx_articles_last_scan.txt` 已复制→验 SHA→删除（备份 `/tmp/wx_articles_last_scan.txt.bak`）。
