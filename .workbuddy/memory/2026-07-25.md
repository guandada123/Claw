# 2026-07-25 工作日志

## 知识库自动化 v8（automation-1782137216020）周六运行
- **修复 discover_gzh_accounts.py 外部发现静默归零 bug**：红狐 API 在 JSON 响应后新增一行营销脚注，原 `_search` 直接 `json.loads(result.stdout)` 抛 JSONDecodeError 被 except 吞掉→返回 []→外部发现恒为 0。改为截取首个'{'到末个'}'之间 JSON 对象。修复后恢复：135 账号 / 3 候选（上海证券报100%、e公司100%、证券时报0%）。
- 新发现真正外部候选号 **e公司**（命中率100%，fakeid 解析失败→pending 待人工接入本地订阅）。已推飞书「🔍外部账号发现 1个候选」。
- merge → signal_ranking.json（28 RSS + 1 发现 e公司 + 2 dedup = 29）；subscription_brief 周报已推（rc=0）。
- 常规挖掘：信号池稳 220（4 小李哥含换行符旧文每轮重处理但去重兜底，0 真新增），0 优质新信号→不推送。
- ⚠️ 遗留：小李哥 4 文件含字面换行符导致 processed-list splitlines 拆分失败、每 6h 空耗算力重扫（信号池仍正确）；e公司 待人工解析 fakeid 后接入订阅方可验证胜率。

## 记忆维护 v8（automation-1780769419635）周六 3:30 运行
- 深度: daily（周六 25号，非周日/非1号）→ 仅 PHASE1 只读检查
- 用户级记忆: SOUL 2468B(80%⚠️) / MEMORY 1834B(89%⚠️) / USER 914B(89%⚠️) / IDENTITY 400B(✅) — 均与7-24持平，无新异常
- 项目记忆: MEMORY.md 健康(3632B)；日日志 16个
- 自动化memory: 全部≤3000B ✅；7天日志: ✅ 完整
- 修复: 无（daily只读）；**[SILENT]**（系统维护类铁律优先，无新🔴异常）
