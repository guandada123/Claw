# 2026-07-26 工作日志

## 21:00 双副本/软链审计（自动化 1783671485008）
- 触发：未提交变更 135 文件（≥10）→ 完整 7 步审计（周日非周五）
- 结论：软链 0 / 同名碰撞 0 / 隐形双副本 0 → 静默退出不推送飞书
- 关键发现：scripts/ ∩ src/claw/ 同名 1（`wind_monitor.py`，md5 分歧=同名异内容良性）；tests/ ∩ .workbuddy/tests/ 同名 2（conftest.py/test_summarize_batch.py，md5 分歧良性）；删 4 文件均 0 自动化引用→安全
- 数据层双 REAL 副本分歧观察项（自 07-16 起）持续结清（根 data/simulation/portfolio.json 07-22 删后保持 ABSENT）
- 遗留：32 个自动化 prompt 路径漂移（scripts/X.py vs .workbuddy/scripts/X.py）建议专项审计

## Wind 配额用量日报 (10:05)
- 运行 wind_quota_report.py --push 成功
- 已用 0/100 (0.0%)，剩余 100 次，充足无预警
- 推送飞书成功

## 🔧 飞书通道自检+维护巡检 (10:09, automation-1784084428353)
- 通道✅ om_x100b69628b0fd8a0b26555a8fa82d5e；告警黑洞✅ send_feishu_error=True
- 双副本审计 09:51 失败=orchestrator 重启中断（非脚本故障），已入健康巡检 FAILED_COUNT=3 自推送 om_x100b6962f14bdca0b3f203628494b32，今晚 21:00 重跑自愈
- 健康巡检 10:03 已自修复 proxy-deepseek(9999) 丢失（连续两日 07-25/07-26，建议查 launchd KeepAlive）；本次实测 :9999=501+PID 25363 存活确认
- 判定：无未处理告警 → [SILENT] 静默退出。lark-cli binary 1.0.76→1.0.77 可升、skills 1.0.68 out of sync 择机 update

## 🔧 prompt 路径漂移专项修复（续 "修复" 指令，09:5x）
- 上轮已精确修复 10 个 cwd=Claw 的 ACTIVE 自动化（验证 0 断链，备份 /tmp/audit_fix_backup/prompts.json）
- 本轮续修 2 个 PAUSED 同病自动化（automation_update 工具无法触达 PAUSED，走 DB 精确替换+备份）：
  - 1780614267086【炒股助理·财报预警】`scripts/sim_trade.py`→`.workbuddy/scripts/sim_trade.py`(1处)；`scripts/cost_tracker.py` 保持(canonical 在 scripts/)
  - 1780964240589【知识库·文章归档索引】`scripts/knowledge_base.py`→`.workbuddy/scripts/knowledge_base.py`(4处)
  - 备份 /tmp/audit_fix_backup/prompts_paused.json；修复后验证两者 0 断链
- 全库 scripts/ 路径漂移断链：仅剩 1784039339114(ACTIVE,重复待删) + 2个 DELETED(忽略)
- 待决：① 1784039339114【盘中·投顾策略执行】与 5 个投顾策略 slot(1784506600526..3706) 重复→应删非修(修则双执行)；② 4 处 MISSING 死引用(wx_morning_report.py 已挪 src/claw/cli/；fetch_gzh_trends/fetch_evening_data/status_aggregator 真缺失，多在 PAUSED)需人工清理决策

## 11:1x 路径漂移收尾（用户确认：删 1784039339114 + 余下按意见处理）
- 1784039339114【盘中·投顾策略执行】：automation_update delete 确认已软删(deleted_at 已置；raw status=ACTIVE 为遗留脏字段，调度以 deleted_at 为准)→ 双执行风险本就未实际发生(07-22 起已软删)
- 4 处 MISSING 死引用复核：fetch_gzh_trends.py(~/.workbuddy/skills/… 绝对路径正确)、status_aggregator.py(cd ~/workbuddy_marvis_bridge 相对解析正确) 均非断链→无需动；仅 2 处真问题已处理：
  - wx_morning_report.py 改指向 `src/claw/cli/wx_morning_report.py`：1782741941693/1782817769722(ACTIVE)+1782741945710(PAUSED) 共 3 处
  - fetch_evening_data.py 真缺失：1782741945710/1782002856945(均 PAUSED) 死命令+后续 cat 步骤标注 [DEPRECATED] 跳过
  - 备份 /tmp/audit_fix_backup/prompts_missing.json
- **最终全库(未软删)自动化 scripts/ 路径漂移断链 = 0**，prompt 路径一致性专项审计闭环

## 15:0x proxy-deepseek 看门狗部署（根治周期性掉线）
- 根因确认：proxy 的 launchd 作业(`com.workbuddy.proxy-deepseek`)被**外部 unload**（重启 WorkBuddy / Marvis 内存守卫 / Colima 启停），其 plist 的 `KeepAlive:true` 只对「进程自己退出」负责，对「作业被 unload」无效 → 空窗直到巡检 `launchctl load` 才救活。07-25/07-26 连续两天同类，14:03 巡检救活后 15:01 又掉。
- 落地看门狗：
  - `/Users/guan/.workbuddy/proxy_watchdog.py`（managed python3 跑，检测 label 是否在 launchctl list + :9999 socket 连通，缺失则自动 load）
  - `/Users/guan/Library/LaunchAgents/com.workbuddy.proxy-watchdog.plist`（`StartInterval=30`, `KeepAlive=false`，每30s巡检，状态变化才写日志去重）
  - 验证：受控 unload→watchdog 30s 内拉回；70s 观察 proxy 恒 200/loaded=1。
- ⚠️ 关键坑（macOS launchd）：后台 agent **不能直接跑 `/bin/bash` 脚本**（报 `Operation not permitted` exit 126），必须像 proxy 自身那样用 managed python3 直跑 .py。初版 bash 脚本踩此坑已弃用。
- 不动原 `proxy-deepseek.plist`。遗留：若 Marvis 内存守卫仍在周期性 unload，可再给它加「重启前先 load proxy」双保险。

## 15:13-15:22 看门狗复查（用户「检查」）
- 复查结论：proxy_watchdog **完全正常**，此前「日志为空」是假象——去重逻辑只记状态变化 + 我反复 `rm` 日志文件造成错觉；实际日志已累积记录 15:01/15:14/15:18 多次真实 unload → 均 2s 内 MISSING→RECOVERED 拉回。
- 确认 proxy 正被外部（Marvis 内存守卫）**反复 unload**，但看门狗秒级免疫，实际 downtime < 2s。
- 优化 log()：① 不再静默吞异常，失败打到 stderr（launchd 捕获进 .stderr.log）；② 加 200KB 轮转（保留末尾500行）。
- 清掉 /tmp/proxy-watchdog.stderr.log 中 28 条旧 bash 残留 `Operation not permitted`（切 Python 前的历史），现 stderr 0 行。
- 35s 连续观察：proxy 恒 http=200 / loaded=1 / watchdog=1。结论：proxy 对重启/清理动作已免疫。

## 18:02-18:3x 健康巡检 + 代理反复掉线根因收敛
- 18:02 巡检发现 proxy(9999)+watchdog **双双未加载**（launchctl 无 workbuddy 项）→ 说明 15:22 之后有外部动作把 **watchdog 自身也 unload 了**（Marvis 内存守卫连看门狗一起摘），看门狗失效后 proxy 无人回拉。
- 18:02 用 `launchctl load` 拉起 → 18:33 复查 **又死**（不在任何 domain）。根因：Bash 工具的 `launchctl load` 注册进**瞬态 session**，随 shell 结束被回收，非持久。
- **修正：改用 `launchctl bootstrap gui/$UID <plist>` + `launchctl enable`**（标准持久注册法）。已对 proxy-deepseek + proxy-watchdog 双注册进 gui/501 域（enabled，PID 47135/47127），:9999 POST→200。
- 遗留风险：若 Marvis 内存守卫周期性 unload 这俩 label，bootstrap 仍可能被摘 → 真正根治需：① 在 Marvis 设置把 proxy/wdog 加入白名单/排除；或 ② 升为 LaunchDaemon(system 域，更抗用户级守卫)。下一步观察 1h 确认是否再掉；若再掉即坐实 Marvis，走白名单。
- 3 个被中断自动化（09:51 orchestrator 重启所致）均为 DAILY 周期任务，会在下一调度点自动补跑（记忆维护 03:30 / 信号溯源 次日05:05 / 双副本审计 21:00），无需手动重跑。

## 18:49 用户确认 Marvis 已修复 → 代理事件闭环
- 用户反馈「马维斯那边已经修复了」（Marvis 内存守卫不再 unload WorkBuddy launchd 作业）。
- 验证：proxy PID 47135 自 18:33 bootstrap 起稳定运行 15min+，`:9999` POST→200；launchctl 状态 proxy=running/never exited、watchdog=not running(last exit 0，符合 StartInterval=30 周期检查设计)；两者均 enabled 在 gui/501 持久域。
- **闭环**：根因=Marvis 周期性 unload proxy+watchdog → 18:33 改用 `launchctl bootstrap gui/$UID`（替代瞬态 `load`）持久注册；Marvis 侧修复后不再摘，代理恢复常驻。
- 经验沉淀：launchd 用户代理持久注册须用 `bootstrap gui/$UID` + `enable`，`launchctl load` 在自动化 Bash 工具里仅是瞬态注册会被回收。

## 21:5x 研报解析 + 入库（用户「存入知识库」）
- 来源：微信转发 PDF《国金证券策略周刊 2026-07-26 超跌反弹进行时，静待不确定性事件落地（c3详细版）》24页文本型(49图)
- 解析：pypdf 提取全文→结构化(市场定位/本周事件/四大主线/中报线索/大类资产/持仓相关性)
- 入库：`.workbuddy/data/reports/guojin_strategy_weekly_20260726.md`（含与用户持仓相关性表：长江电力✅主线共振、紫金⚠️资源品、封测🔶间接待7/30美联储、美的⚠️非主线）
- 核心结论：沪指3741三重探底缩量企稳；本周关键=长鑫上市(周一)/美联储7/30/政治局会议/美股AI财报；四大主线=人工智能·电力·证券·创新药；中报预喜43% K型分化(非银88.89%高增)；操作"均衡分批、事件落地后低吸，7/30前不重仓追高AI"

## 20:00 【炒股助理】下周前瞻自动化 (automation-1780651521972)
- 执行✅ 卡片推送 om_x100b696b10802cb8b36be3c435e95e2；交付 output/weekly_outlook_2026-07-26.md
- 依赖全部到位(pool_health/macro_weekly/signal_weights)；portfolio.json 未在 $DATA，持仓改以 pool_health.holdings_detail 为准
- 核心：宏观6/10政治局会议(07-28~31)博弈；池健康2/10偏弱(-2.04% 9涨23跌)；⚠️ 实盘长电(-8.35%)/华天(-10.74%)已破-8%止损纪律线→建议周一硬止损；模拟盘三只安全持有；半导体材料逆势唯一正大类
