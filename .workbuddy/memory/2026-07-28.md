# 2026-07-28

## 💰 成本看板「打开 404」根因修复（23:00-23:13）
- 用户反馈成本看板飞书 Drive 链接打开 404。定位：`cost_dashboard_feishu.py`
  - **根因1（跨域）**：`LARK_DOMAIN` 硬编码 `bytedance.feishu.cn`，但 lark-cli 登录的是普通飞书（`brand: feishu`，主群 oc_9ee... 在 feishu.cn 域）→ 链接跨域打不开，跳登录墙（curl 显示 HTTP 200 但 302 到 accounts.feishu.cn/login，误判为 404）。
  - **根因2（权限）**：飞书 Drive 文件默认私有，需「链接分享」权限；lark-cli `drive +member-add` 仅支持指定成员授权、无「链接公开可读」CLI 入口（且需 --yes 确认），**方案 A（改域+开链接权限）在 CLI 能力边界内走不通**。
- **最终方案 B（落地）**：弃用 Drive 链接，改为 `lark-cli im +messages-send --chat-id <群> --file <本地HTML> --as bot` 把仪表盘作为**群文件**直发主群。群成员天然有权限打开，零 404/登录墙。
- 关键改动（`scripts/cost_dashboard_feishu.py`）：
  - `LARK_DOMAIN` → `feishu.cn`（虽最终不用链接，但保留正确域）
  - 删除 `upload_to_drive` / `add_link_view_permission` / `build_file_url`( Drive 版)
  - 新增 `send_dashboard_as_file(file_path, chat_id)`：复制 HTML 到 cwd 用相对路径（--file 拒绝对径）→ `im +messages-send --as bot`
  - `--as bot` 原因：user 身份缺 `im:message.send_as_user` scope（authorization missing_scope）；bot 身份 `im:message` 已就绪，发文件成功
  - `build_message` 链接行改为「完整仪表盘已作为群文件发送，请查收本群附件」
- **验证**：完整 `main()` 真跑一次 → 群文件发送成功 + 卡片推送成功（message_id=om_x100b69b83ec79ca4b4b31ac9590a342）。今日 22:10 自动化（automation-1784255402639）即按新逻辑运行。
- 铁律遵守：改脚本先 --dry-run 思路，验链路仅 1 次真实推送（端到端）。

## 🧩 沉淀 skill：feishu-drive-file-push
- 把「飞书文件/仪表盘推送打不开(404/登录墙)排错 + 群文件直发方案」存为用户级 skill：`~/.workbuddy/skills/feishu-drive-file-push/SKILL.md`（v1.0.0）。
- 覆盖：跨域(bytedance vs feishu)、Drive 链接权限 CLI 走不通、改用 `im +messages-send --as bot` 群文件直发、--file 拒绝对径、user 缺 im:message.send_as_user scope 等坑。下次同类问题直接触发此 skill。

## 🔧 增强 feishu-channel-selfcheck skill
- 给 `feishu-channel-selfcheck` 加 **Step 4.6 成本看板推送退化检测**：扫描 `cost_dashboard_feishu.py` 等「生成HTML→飞书」脚本是否退化回 `drive +upload` / `bytedance.feishu.cn` / `feishu.cn/drive/` 链接，命中即告警（按 feishu-drive-file-push 方案 B 修复）。
- frontmatter 描述、铁律关联引用、常见坑表、Step 5 报告项均已同步更新。实测当前脚本 ✅ 通过退化检测。
- 目的：防止本次修复被后续改动回退（跨域 404/登录墙回归）。
