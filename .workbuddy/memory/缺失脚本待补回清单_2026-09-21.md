# 缺失脚本待补回清单（2026-09-21）

> 背景：14:1x 轮因环境缺口重建了 10 个脚本（5 忠实 + 5 SAFE-MODE）。
> 本清单列出 **SAFE-MODE 脚本缺的原实现/凭证** 以及 **忠实重建脚本的隐含缺口**，
> 供后续"恢复权威行为"时逐项处理。所有脚本目前均已就位、可执行、语法通过、功能不报错——
> 即"不再降级"已达成；本清单是"从能跑到权威"的下一步。

## 优先级速览

| 优先级 | 脚本 | 缺口性质 | 现状 |
|---|---|---|---|
| **P0** | discover_gzh_accounts.py | 外部 API 凭证缺失，功能完全不可用 | 恒返回空 |
| **P1** | advisor_rules.py | 原规则阈值/半导体转折逻辑缺失 | 仅规则A保守 warn |
| **P1** | run_debate.py | 原辩论后端+上游行情缺失 | 恒 HOLD/0.0 |
| **P2** | merge_signal_ranking.py | 原加权多源打分缺失 | 功能可用、欠精细 |
| **P2** | subscription_brief.py | 原 LLM 摘要渲染缺失 | 功能可用、仅透传 |
| **P2** | is_trading_day.py | 精确节假日+调休空白 | 近似表、调休留空 |
| **P2** | cost_tracker.py | cost_estimates.json 缺失 | 成本记录恒 skip |

---

## P0 — 功能完全不可用（需凭证/原实现才能恢复）

### 1. discover_gzh_accounts.py
- **类型**：SAFE-MODE（重建）
- **缺什么**：外部「红狐 API」(redfox) 的凭证/端点（账号发现 + 股票提取 + 方向验证 + 命中率）。
- **影响链路**：`全网挖掘(automation-1782137216020)` 的**周六三步骤**公众号账号发现 → 当前恒返回 `accounts: []`。
- **现状**：`mode: dependency_missing`，exit 0 非阻断；但账号发现实质为空。
- **补回方式**：补回 redfox API 凭证/端点，或恢复依赖该 API 的原脚本。
- **备注**：RSS 自 08-19 断供（第 37 天），整体挖掘早已停摆；此脚本恢复仅解决"账号发现"一环，仍需 RSS/Wind 等上游恢复才完整。

---

## P1 — 已能跑但行为被简化（恢复权威需原实现）

### 2. advisor_rules.py（持仓纪律引擎）
- **类型**：SAFE-MODE（重建）
- **缺什么**：原 `AdvisorRules` 的 **A/C/B/D/E 规则阈值**，以及**半导体转折判定**（RPS / MA20 / 动量）。
- **影响链路**：`助理实盘监控 (PHASE3.5 纪律引擎)`、`投顾策略执行` 持仓风控。
- **现状**：仅实现规则 A（止损破位 → 保守 warn），缺 risk_note(C) / 行业集中度(B) / 半导体转折判定；不输出任何确定性买卖指令。
- **补回方式**：恢复原 advisor_rules 源码（含规则表与转折判定）。
- **严重度**：中（已能跑不报错，但纪律细节缺失，长电/华天这类破位标的只给"请人工复核"，不再自动分级）。

### 3. run_debate.py（多空辩论共识）
- **类型**：SAFE-MODE（重建）
- **缺什么**：原**多智能体辩论后端**（行情 / LLM 辩论通道）+ **上游行情拉通**（原通道行情数据此前亦未拉通，属已知故障）。
- **影响链路**：`投顾策略执行`、`午间选股` 的买卖共识。
- **现状**：恒返回 `consensus: HOLD / confidence: 0.0 / safe_mode: true`。
- **补回方式**：恢复原辩论后端实现，并先修复上游行情拉通故障。
- **严重度**：中（共识退化为恒 HOLD，但当前投顾档多 SILENT，影响有限）。

---

## P2 — 功能可用、仅欠精细/监控（改善型，可选）

### 4. merge_signal_ranking.py（信号排名合并）
- **类型**：SAFE-MODE（重建）
- **缺什么**：原**加权多源打分合并**逻辑（重建仅"拼接 + 按 score 降序"）。
- **影响链路**：`全网挖掘` / `演进` 信号合并。
- **现状**：功能可用、合并正确，但欠精细（无跨源权重）。
- **补回方式**：恢复原实现（可选，非阻断）。

### 5. subscription_brief.py（订阅简报生成）
- **类型**：SAFE-MODE（重建）
- **缺什么**：原 **LLM 摘要 / 排版渲染**（重建仅读取并透传 items）。
- **影响链路**：`订阅简报` 生成。
- **现状**：功能可用、透传 items，但无摘要/渲染。
- **补回方式**：恢复原实现（可选，非阻断）。

### 6. is_trading_day.py（交易日判定，忠实重建但隐含缺口）
- **类型**：忠实重建（逻辑在，数据近似）
- **缺什么**：① `HOLIDAYS_2026` 为"公开口径近似"，非交易所官方精确表；② **调休补班开市日**明确留空（注释：2026 安排待官方公布）。
- **影响链路**：**所有按交易日门控的自动化**（实盘监控 / 投顾策略执行 / 选股 / 记忆维护等）。
- **现状**：2026 主要节假日已覆盖（实测 09-21→交易日、09-25/10-01→非交易日正确），但**调休边缘日可能误判**（把补班开市日当休市、或反之）。
- **补回方式**：补交易所官方休市表 + 调休补班配置（可做成可维护的 JSON，而非写死）。
- **严重度**：低-中（仅调休日边缘场景；若今年调休日恰有自动化槽位则需修正）。

### 7. cost_tracker.py（成本追踪，忠实重建但隐含缺口）
- **类型**：忠实重建（DB/逻辑在，缺估算配置）
- **缺什么**：`<CLAW>/.workbuddy/data/cost_estimates.json`（自动化名 → token/成本估算映射）。
- **影响链路**：所有 `done_schedule` 的成本记录。
- **现状**：配置缺失时打印"未找到估算配置，跳过成本记录"，exit 0 非阻断；即成本监控目前是 **no-op**。
- **补回方式**：补 `cost_estimates.json`（各自动化名 → model / in_tokens / out_tokens / cost 估算）。
- **严重度**：低（仅成本监控缺失，不影响业务自动化）。

---

## 建议执行顺序
1. **P0**：若全网挖掘要恢复 → 先解决 redfox 凭证（但需连同 RSS/Wind 上游一起评估）。
2. **P1**：若要让实盘监控/投顾有"权威纪律与共识" → 恢复 `advisor_rules.py` + `run_debate.py` 原实现（run_debate 还需先修上游行情）。
3. **P2**：想让监控/判定更稳 → 补 `is_trading_day.py` 精确表+调休、补 `cost_estimates.json`；`merge_*`/`subscription_*` 为改善型，可后置。

---

## 2026-09-21 下午 补充：54文件同步缺口闭环（修正"P1 done"叙事）

> 14:1x/14:3x 的"P1 done"**不完整**：`advisor_rules.py` / `run_debate.py` 虽已从 root 恢复权威原版，但其**依赖模块当时仍缺失** → 实际 `import` 会失败，行为仍降级。

### 根因：root `scripts/` 比 `.workbuddy/scripts/` 多 54 个文件（系统性同步缺口）
扫描 `.workbuddy/scripts/` 全部文件的 import，定位 **14 个 root-only 模块被现存脚本引用**（这才是 hub/P1 真正缺的部分）：

| 缺失依赖 | 被谁引用 | 性质 |
|---|---|---|
| market_sentiment / price_sanity / sector_strength_layer / t0_strategy | advisor_rules.py | 纪律引擎依赖 |
| anysearch_helper / qts_client | run_debate.py | 辩论后端依赖 |
| budget_guard / check_no_double_import / qts_client / signal_consensus / source_weight | unified_ops_center.py（中枢编排） | 中枢降级根因 |
| cross_portfolio_monitor | sim_signal_advisor.py | 监控依赖 |
| refresh_portfolio | validate_constraints.py | 校验依赖 |
| router | cost_tracker.py / read_wx_articles.py | 路由器依赖 |
| tailscale.sh | health_check.sh / health_check.py | 健康检查 shell |

### 处置（已完成）
- 已从 root `scripts/` 同步上述 **14 个（13 py + tailscale.sh）** 到 `.workbuddy/scripts/`，均为**权威原版**（0 SAFE-MODE 标记）。
- 验证：import 冒烟 0 fail；advisor_rules+debate 测试 **67 passed**；重扫"仍引用但缺失的 root-only 模块" = **NONE** → 闭环。

### 修正结论
- **P1 现真正完成**：advisor_rules / run_debate 既恢复权威原版、又补齐依赖，可正常 import/运行。
- **hub 中枢(unified_ops_center.py) 降级根因消除**：5 个缺失依赖已到位。
- 剩余 **40** 个 root-only 模块经核实**不被任何 `.workbuddy` 脚本引用**（dev_only / backend / 凭证类：alpha_*、bt_*、local_model、legacy_secrets、cost_dashboard*、context_compressor 等）→ **有意不同步**，避免引入敏感配置与重依赖。
- 注：hub 卫生审计/演进自测的"降级→完整执行"在 **14:1x** 已由 `skill_hygiene.py`+`workspace_scan.sh` 重建达成；本次是更深一层的"中枢编排 + 纪律引擎依赖完整性"闭环，非重复劳动。

---

## ⚠️ 2026-09-21 17:5x 【已验证 erratum】本清单全部 P0/P1/P2 缺口经实测为 Phantom

> 编写本清单时未重新核验运行文件，采信了 14:1x 的旧叙事（"SAFE-MODE 重建/简化版"），
> 逐项断言均与当前实际文件不符。这正是今早自审"依假设状态而非已验证状态行动"的反面教材。
> 以下为 17:4x–17:5x 实测结果，**结论：当前无任何脚本降级/缺失/凭证缺口，无需补回**。

### 实测证据
| 清单断言 | 实测（17:4x） | 结论 |
|---|---|---|
| P0 discover_gzh 缺红狐凭证→恒返回空 | `REDFOX_API_KEY` env=<set>（已设）；脚本走 `localhost:5001` 本地 API，非直连红狐 | 非缺口（env 已设；真实返回需实跑确认） |
| P1 advisor_rules 仅规则A | `.workbuddy/scripts/` 版与 `root/scripts/` 权威原版 `diff -q` 一致（1377行全量含 A–E/半导体/RPS/MA20） | 非简化版 |
| P1 run_debate 恒HOLD/0.0 | 同上一致（758行含多专家辩论后端/consensus） | 非简化版 |
| P2 merge_signal_ranking 缺加权 | 同上一致（含 `weighted_score=胜率×信号量`） | 非缺口 |
| P2 subscription_brief 缺LLM渲染 | 原版即统计简报，本无 LLM 内容渲染 | 非降级 |
| P2 is_trading_day 近似表/调休留空 | 实际读 `data/astock_holidays.json`（存在，2026全年准确，last_updated 2026-06-19），仅调休补班边缘日未单列 | 已基本解决 |
| P2 cost_tracker cost_estimates.json 缺失→no-op | 该文件**不存在且无任何 .py 源码读取**；真实配置是 `cost_tracker.py:389` 内联 `AUTO_COST_ESTIMATES` dict（已含盘前/收盘/早报/复盘等）；`log_estimate` 未命中时打印"跳过成本记录"并 `exit 0`（优雅 no-op，非失败） | Phantom（建该文件=建无人读的文件） |

### 交叉佐证（来自自动化自身运行记录）
- 全网挖掘(1782137216020) 今日 02:41/08:42/13:12/15:17 均 SILENT 正常；13:12 曾误标"三脚本缺失"，**15:17 已自纠**："三脚本缺失已由14:1x SAFE-MODE重建补齐"。证明脚本本就在位。
- 信号溯源 今日随链跑 x2（含 -pm 变体），见补推(1783469126245) memory。
- done_schedule 旧记 RC=1 "成本配置缺失" 实为 `log_estimate` 优雅 no-op（exit 0），非失败。

### 处置建议
1. **本清单所列 P0/P1/P2 补回项均无需执行**——当前运行文件已是权威版，无降级。
2. 若需"权威纪律/共识更稳"或"成本监控更全"，属**可选增强**（扩展 `AUTO_COST_ESTIMATES`、补 `astock_holidays.json` 调休表），且涉及估算值/外部数据，需另行确认，**不应视为"补回缺失"**。
3. 保留本 erratum 警示后续会话：任何"X 缺失/降级"结论须基于 `ls`/`diff -q`/实跑验证，不得采信旧摘要。
