# 2026-07-17

## 💰【成本看板】飞书成本仪表盘日报自动化首推（22:10, automation-1784255402639）
- ✅ 仪表盘生成（26.5 KB）→ 上传飞书Drive `HWZmbYpB2ozSomxoZFbcEZbendh` → 推送主群 ✅ message_id=om_x100b6a9725781ca8debb3f7316bcce6
- ⚠️ `$PYTHON` shell 变量权限问题：脚本无执行位，用 `/Users/guan/.workbuddy/binaries/python/versions/3.13.12/bin/python3` 绝对路径成功
- 自动化 memory 已创建（首次记录），schedule done

## 📅【持仓】财报日历（automation-1783742027380, 07:50）
- 运行 `earnings_calendar.py`：实盘2只(长电科技/华天科技)，均无今日披露
- 长电科技中报 08/21 | 华天科技中报 08/22（与上周一致）
- 今日无披露事件 → 静默退出 ✅（非周一，跳过周报）

## 记忆维护（automation-1780769419635, daily, 周五）
- 🔧 **修复：项目 MEMORY.md 蒸馏瘦身** — 因超出注入上限被截断(16041B)，按 ACTION REQUIRED 合并去重重写：三系统边界/身份表合并、技术债✅压缩、经验教训并入存储架构段。结果 **8352B（-48%）**，所有铁律/防回退/近期架构(双轨信号源/致态SSD)均保留。
- 用户级记忆：SOUL 2468B(80%⚠️) / MEMORY **2857B(🔴>2KB软阈值，但注入内未截断，跨项目文件，未擅改待用户裁)** / USER 914B(89%⚠️) / IDENTITY 400B(✅)
- 项目日日志：7个（本次补建 7-17）；自动化 memory 无>3000B；7天连续性仅缺今日本次补建
- 推送：按核心规则「系统维护类不推送」**未推飞书群**（维持 7-15/7-16 决议，prompt推送指令与核心规则冲突仍待用户裁）

## 🇺🇸 美股监控收盘（automation-1780615006148, 04:26 GMT+8）
- 美东7/16周四收盘：道指 -0.20% / 纳指 -1.47% / 标普 -0.51%；龙头 AAPL+1.76% / NVDA-2.39% / TSLA-0.86% / MSFT+1.37% / AMD-5.33%
- 主线：芯片再遭抛售(台积电上调Capex至600-640亿致股价-3%, 费半-3.5%, AMD-5.3%, 美光-6%, SK海力士-11%)；七巨头分化(苹果微软撑盘, 谷歌-4.4%推迟Gemini 3.5 Pro)；中概金龙+2.5%(阿里+5%)
- 沉默判定：纳指-1.47%破±1% + 重大板块轮动 → 非静默，已推飞书群 message_id=om_x100b6aa7a99d14a8c2e19d3cd8aa86e
- A股映射：半导体/封测情绪承压(长电/华天持仓)；中概消费利好；油价破85利好油气利空航空

## 🩺 风险巡检（automation-1782035436209, 08:37）
- 写入 `.workbuddy/data/risk_snapshot.json`（6条：0高/4中/2低），已推飞书群(om_x100b6aa3ddf81cacc0b696bc417ed9f)
- 4中：①市场连跌2日确认downtrend(7/16 上证-1.85%/深证-1.97%/创业板-2.95%) ②长电+华天同属封测链强相关集中 ③10/11号命中率<50%低权重信号泛滥 ④portfolio汇总position_pct79.4%与明细≈29%差额≈6万(7/15调仓同步疑漏)
- flags: stop_risk=False / market_downtrend=True / automation_issues=True(4🟡) / signal_gap=False(最新07-16)

## 🐟 鱼盆数据提取（automation-1783472286775, 08:50）
- 选中文章：`不知不觉中，已在慢慢变好…`（猫笔叨的读后感专区），评分 181
- 数据日期：**2026-07-15**（相对 7/16 批次 2026-07-14 为新数据，滞后发文日2天）
- 6张图片OCR：图1=鱼盆趋势20行 / 图2=板块轮动14行（其余4张为房价表/K线/美股/加密币，非数据表）
- 输出：`yupen_2026-07-15_sector_rotation.json` + `yupen_2026-07-15_yupen_trend.json`
- 关键观察：板块轮动强势CS创新药+12.18%居首，趋势模型港股三剑客(恒生/恒科/国企)领涨；今日状态转变：板块=证券公司(0.64%黄底)、指数=科创50(-4.61%黄底)
- 推送：飞书群 message_id=om_x100b6aa39d16fcb4de255e125f0736b

## 📈 智能选股（automation-1780738597945, 09:00）
- 账户：模拟盘 ¥32,737(+9.1%) | 现金¥13,073 | 美的/长电/紫金 → 金融空白
- 三方案六维评分：A三一重工600031 400股 6.43 / B招商银行600036 200股 7.43✅ / C伊利600887 300股 6.55
- 已选B：招商银行 600036 / 200股(100倍数) / 买区37.00–38.20 / 止损34.66(-8%) / 目标39.50(+5%) / 占¥7,534留现¥5,539
- 风控：行业≤60%✅ 同日1仓✅ 留现≥15%✅ 100倍数✅
- 写入 experiments/2026-07-17.json + /tmp/buy_plan.json；推送✅ schedule done✅ cost_tracker✅(kimi-k2.6≈¥9.6)
- 坑：is_trading_day.py --next 不支持；自写循环原用`'交易日' in stdout`误判(非交易日含子串)→改 exit code，下交易日=2026-07-20，已记防周五重犯

## 🐟 鱼盆新鲜度核验（09:22，QA 复核）
- 用户转来飞书鱼盆通知「数据日期07-15 + 猫笔叨《不知不觉中…》」，初判疑似 OCR 卡在 pending_ocr / 07-16 漏结构化。
- 核验磁盘后**推翻初判**：①`yupen_2026-07-15_*`(5054/7023B, 08:54) 与 `yupen_2026-07-14_*`(07-16 08:52) 均正常存在；②`yupen_2026-07-17_raw.json` 的 `status:pending_ocr` 只是 `fetch_yupen_rss.py` 原始抓取占位，真实结构化落在 `data_date` 命名文件；③自动化 1783472286775 自身 memory 证实 07-17 批次已完整 OCR+推送。
- 根因：结构化文件名用的是**数据日期(07-15/07-14)**而非运行日期，grep `*2026-07-16*sector*` 自然无命中→误判漏数据。
- 结论：**无数据缺口，无需补跑**。reader --freshness-only → stale / data_date=2026-07-15（滞后2天，正常）。早报取最新有效 JSON 正常。

## 🐟 鱼盆 7/17 文章补抓 + 逻辑修正（09:27–09:31）
- 用户提供 7/17 新文 `https://mp.weixin.qq.com/s/vpA358I4R4k6CvQ-Wuspvg`《完了，猫哥也开始悲观了…》，验证后确认**确实漏抓**：该文章未在任何 raw/structured 文件出现，自动化 1783472286775 08:50 批次选中的是 7/16 文章（`95sPXrj8…`）。
- 补抓：下载 3 张图片，写 `yupen_2026-07-17_raw.json`（backfilled=True），LLM 视觉 OCR 提取表头日期 **2026.07.16** 的两张表：
  - `yupen_2026-07-16_sector_rotation.json`：14板块，状态转变（黄底）= 房地产（+0.54%，26.07.16）+ 证券公司（-1.15%，26.07.16）；强势前三 CS创新药(+10.52%) / 中证消费(+5.43%) / 中证煤炭(+1.76%)。
  - `yupen_2026-07-16_yupen_trend.json`：20指数，状态转变 = 微盘股（+0.96%，26.07.16）+ 上证50（-1.53%，26.07.16）；强势前三 恒生科技(+5.85%) / 国企指数(+5.65%) / 恒生指数(+5.39%)。
- 文件名修正：raw 用**抓取日期**、structured 用**数据表头日期**的约定写入 `MEMORY.md` 防回退段，并新增补抓命令文档。
- 脚本修正：`.workbuddy/scripts/fetch_yupen_rss.py` 新增 `--article-id <URL>` 参数，支持跳过 RSS 列表直接补抓指定文章；已测试错误路径（no_data 生成）。
- reader 验证：补抓后 `read_yupen_data.py --freshness-only` → `stale / data_date=2026-07-16`（从 07-15 推进到 07-16，正常 1 天滞后）。
- 按「系统维护类不推送」原则，本次补抓**未推送飞书**，仅更新 `output/yupen/`；早报/晚报自动化下次运行会消费到最新 07-16 数据。

## 🔴 待裁清单：系统维护类推送冲突（09:47）
- 用户授权整理「系统维护类不推送」与早报/维护自动化 prompt 内 push 指令冲突。DB 查实：多个 ACTIVE 维护类 `push_to_wechat=1` 但运行被 prompt 压制未推。
- ACTIVE+push=1 维护类 8 条：`1780769419635`记忆维护 / `1782002819199`成本监控 / `1782002834355`工程质量 / `1781780654327`综合健康 / `1784084428353`飞书通道自检 / `1781663740184`Quant管线 / `1782394153045`数据备份 / `1783784056233`重启提醒。
- 易混淆（非维护类，勿动）：`1782035436209`风险巡检 / `1783742027380`财报日历 实为投资/项目类应保留推送。
- PAUSED+push=1 遗留：`1780759171289`/`178079008345`/`1781220962599`。
- 已写入 `MEMORY.md` 防回退段「🔴 待裁」，建议落地：①prompt 顶层加 `IF 系统维护类: push=0` 统一压制；或 ②逐条 push 置 0 对齐。当前维持「运行不推」，待用户拍板。

## 💰 成本监控（automation-1782002819199, 09:16 精简告警模式）
- 今日 ¥22.70（7次；reasoner ¥9.60 + kimi ¥9.60 + v4-pro ¥3.04 + flash ¥0.46）→ ¥25 线 90.8%（信息级提示，未破）
- 本月 ¥195.69/¥400（217次），剩余 ¥204.31，预估月底 ¥345 ✅ 预算内
- 本月Top3: 公众号信号溯源 ¥67.20(34.3%) / 智能选股 ¥51.00(26.1%) / 美股收盘回顾 ¥39.52(20.2%)
- 最高模型占比 deepseek-reasoner 34.4%（<50%，无频率异常）
- **[SILENT]** 静默退出：单小时仅04/09历史¥9.60级深度任务误报（已知¥8阈值<单任务成本设计缺陷，同模式复发不重复推送）；今日/本月/模型占比均未达降级线，无降级方案（降级需人工确认，未触及）。
- 环境：显式 `export CLAW=/Volumes/ZHITAI/WorkBuddy/Claw` + `source ... >/dev/null 2>&1`；`log_estimate "成本监控"` 记¥0.135 成功；未置每日锁（保障21:xx日报槽）。
- 遗留待办：①每日锁改6小时级锁（根治每日锁屏蔽后续运行）②¥8单小时阈值上调至¥20（根治深度模型任务误报）。

## ✅ 维护类推送冲突 — 最终落地核验（10:xx）
- 用户决策「维护类噪音5条，有异常再推送，每日重启提醒删除」已全量执行并核验：
  - `1783784056233` 每日重启提醒：soft-delete 成功，`deleted_at` 已置，DB 不再 SELECT 到（前两轮 delete 调用均返回 success 但被「未过滤 deleted_at 的 SELECT」误判为未删，实为已删）。
  - 7 个 ACTIVE 维护类自动化 prompt 顶层统一加「🚨 系统维护类铁律（禁改）：默认不推送飞书群；仅 ⚠️/🔴 异常/失败/主动修复才推送，正常 [SILENT]」：
    - 首轮4：`1780769419635`记忆/`1782002819199`成本/`1782394153045`备份/`1781663740184`Quant管线
    - 次轮3：`1781780654327`综合健康/`1782002834355`工程质量/`1784084428353`飞书通道自检
  - push_to_wechat 维持现状（部分=1 但被 top-rule 压制，与「运行不推、异常才推」一致）。
  - 易混淆非维护类 `1782035436209`风险巡检/`1783742027380`财报日历 保留推送（勿动）。
  - PAUSED+push=1 遗留 `1780759171289`/`178079008345`/`1781220962599` 未动（恢复前需裁）。
- MEMORY.md「🔴 待裁」段已改写为「✅ 已裁」并记落地结果。

## 🔧 成本监控 v9 根治改造（10:13）
- 用户指令「两个遗留待办一起改掉」已落地并端到端验证：
  1. **每日锁 → 6小时级锁**：`schedule_utils.py` 新增 `--interval-hours N` 参数，`_lock_name()` 按 `当前小时//N` 生成槽位锁文件（如 `claw_lock_成本监控_20260717_h6_s1`）。不带该参数时维持原每日锁（兼容其它7个维护自动化）。验证：同槽 check→done→recheck = 0→0→1（正确）；每日级锁独立不受影响。成本监控 prompt 的 `check_schedule`/`done_schedule` 均加 `--interval-hours 6`，根治「每日锁屏蔽同日后继6h运行」。
  2. **单小时阈值 ¥8 → ¥20**：`PHASE 2` 第39行改。原¥8 < 单任务成本（kimi/deepseek-reasoner 单次¥9.60级），深度模型任务必误报；上调至¥20根治。
  3. **连带修 prompt 错误 subcommand**：原 `cost_tracker.py summary`/`daily-summary` 及 `cost_monitor.py summary` 均不存在（v1.0仅 daily_report()）。非22:00 改为 `daily`/`monthly`/`top 10`；22:00 额外加 `estimate_today`。
- 自动化已升 v9（automation-1782002819199）。`schedule_utils.py` 语法+行为测试全绿。
- 风险：无（向后兼容，其它调用方不传 --interval-hours 则仍是每日锁）。

## 🔍 「一起办了」复核（10:23）+ schedule_utils stats 增强
- **第1项（3个PAUSED裁不裁）实为虚惊**：sqlite 核验发现 `1780759171289`(全局记忆梳理)/`1781220962599`(持仓自动刷新) 早在 1783819xxx 时间戳就已 soft-delete（`deleted_at` 已置，`view` 报 not found 即此因）；`178079008345`(每月深度审计) DB 根本不存在。→ 无待裁，不动。
- **第3项 cost_monitor.py 误标更正**：原 MEMORY 第7行写"v1.0旧壳、禁引用"是错的。实测它有 `daily|monthly|summary|dashboard` 四子命令，`cost_dashboard_feishu.py:203` 依赖它生成飞书看板日报。**不能删、非废弃**。已改 MEMORY 第7行修正为"报告层、可引用"，并注明看板链路当前无 ACTIVE 自动化调用（死链）。
- **第4项 schedule_utils.py stats 增强**：`cmd_stats()` 现区分每日级锁 vs 间隔级锁(6h槽)，6h槽显示覆盖时段（如 s1=06:00-11:59）。验证：86锁/今日12（11每日+1 6h槽）显示正确。测试锁已清。
- **结论**：今日待办全清。成本监控 v9 + 6h锁 + stats 增强均落地验证；维护类冲突闭环；3个PAUSED实为已删虚惊。

## 💰 飞书成本看板自动化上线（10:30）
- 用户「可以」→ 接上死的 `cost_dashboard_feishu.py` 看板链路，建定时自动化 **automation-1784255402639**「💰【成本看板】飞书成本仪表盘日报(22:10)」：
  - 每日 22:10（接成本监控22:00日报后，含当日全量）；rrule `FREQ=DAILY;INTERVAL=1;BYHOUR=22;BYMINUTE=10`（修正创建时 DAIILY 拼写错误）；model=deepseek-v4-flash（轻量执行）；cwds=Claw根。
  - 分类=投资/项目管理类（非系统维护）→ 正常推送主群 oc_9ee5303497f5e0e71666b610d6bdc346，不受 anomaly-only 铁律约束。
  - prompt 调 `cost_dashboard_feishu.py --chat oc_9ee5303497f5e0e71666b610d6bdc346`，失败→push_feishu 异常告警。
- **端到端验证**：lark-cli 可达 ✅；dry-run 生成26.5KB仪表盘 ✅；--no-upload 真实发消息到群 ✅（message_id 已得）。中途遇飞书 HTTP 429 限流（非配置错，退避后成功）。
- **连带根治**：`cost_dashboard_feishu.py` 的 `send_feishu_message()` 原无重试→429直接失败。新增指数退避重试（3次，5/10/20s），`import time` 补齐。语法+推送复测全绿。
- 测试残留：群内发了2条 --no-upload 文字摘要（验证用，无Drive链接），已告知用户。

## 🔧 按建议收尾（10:35）— 推送验证纪律固化
- 用户「按你的建议来处理」→ 采纳上轮两条建议：
  1. 成本看板自动化 prompt 加「🔧 验证约定（防群污染）」段：日常调试一律 `--dry-run` 本地验证；确需验链路才 `--no-upload` 且每改动≤1次；正式跑走全量。
  2. 群内2条测试消息**不补发说明**（补一条更刷屏），仅记忆留痕。
- MEMORY.md 核心规则区新增「推送验证纪律」长效条：脚本验收先 `--dry-run`，禁群内反复发测试消息（飞书机器人消息不可API撤回）。

## 📋 飞书推送卡片化改造（11:29 起，按建议 A+B）
- **审计报告**：33个 ACTIVE 自动化，推送全走 `notify_center.py`→`push_feishu.sh`→`lark-cli --markdown`（纯文本卡片，无配色/分区/表格）；`notify_center.py:63` 降级路径用 `--text` 纯文本**丢格式**；早报曾试 Card2.0 因 `lark-cli` 校验失败回退 markdown。根因=没用 interactive 卡片。
- **建统一中台 `scripts/push_card.py`**（替代纯 markdown/text 推送）：
  - `build_card()`：语义配色（level→template: alert=red/warning=orange/info=blue/success=green）+ 分区块（div+hr）+ 可选表格（markdown table）+ 可选按钮（主操作入口，飞书当前点不动仅展示）+ 页脚。
  - `send_card()`：带 429 指数退避（3次5/10/20s）+ **markdown 兜底（保留格式，绝不用 --text 丢格式）** + 30KB 大小检查。
  - CLI：`--title/--level/--section(可重复)/--table-headers/--table-rows/--button/--footer/--json-stdin`。
- **🔧 关键修复**：`lark-cli 1.0.68` **无 `--card` flag**（早报230001根因）。正确方式=`--content '<card_json>' --msg-type interactive`。已改 `_send_via_lark()` 用此方式。
- **验收**：单元测 build_card（7 elements/884B 合法JSON）+ 真实发早报风格卡片到主群 ✅ `message_id` 已得（蓝头/分区/表格/按钮渲染正常）。群里现多1条样板卡片（验收必需真实验证，每改动≤1次群发纪律已守）。
- **下一步（待用户验收后推广）**：早报自动化(1782741941693)改调 push_card；其余按 投资类→维护类告警→其余 顺序迁移；最终可废弃 notify_center 降级 --text 路径。

## 📊 早报卡片化改造（11:45，方案 X 自治）
- 用户「X」→ 早报推送走「脚本内部自治生成 docx + 取真实 url」方案，不依赖外部传参。
- **建通用脚本 `scripts/push_morning_report.py`**（替代每天一个的 `_push_morning_20260717.py` 技术债）：
  - 读 `output/wx_reports/{today}_morning.md`（支持 `--date YYYYMMDD` 调试）
  - 复用 `_create_wx_doc.py` 逻辑：`lark-cli docs +create`（user→bot 降级）生成飞书文档，取真实 `url`
  - 解析 md 标题层级 → 卡片 sections（_parse_md_to_sections，风险段含🔴→level=alert 红头）
  - 调 `push_card.py` 发 interactive 卡片，按钮「📄 完整报告」→ 真实 docx url（非假链接）
- **端到端验证**：真实建 docx `FkwWdJVTxo8cz9xLwXocT8Ecnme` + 发卡片 `om_x100b6aae1671c4a4c2c105c7f21c217`（11区块/红头）✅。群内现多1条早报卡片（验收必需真实验证，每改动≤1次群发纪律已守）。
- **早报自动化 `automation-1782741941693` prompt 改推送段**：删旧 `--text` 内联卡片逻辑，改调 `push_morning_report.py`，注明脚本行为（自治建文档/红头判定/markdown兜底）。
- **修复根因**：旧早报脚本第 68 行用 `--text` 纯文本推送（你"看的不舒服"主因）；docx 链接早就有（真实短链），只是没用按钮。
- 遗留：其余投资类（晚报/选股/美股）+ 维护类告警 待按同法迁移到 push_card；notify_center 的 `--text` 降级路径待废弃。

## 🐟 鱼盆数据时效修复（12:17，方案 B 微调）
- 用户质疑「早报鱼盆还是15号」→ 根因定位：**调度时序**：鱼盆提取自动化(1783472286775)原 9:00 跑 OCR，早报(1782741941693) 9:15 跑；但 7/17 文章是用户 09:16 才给 URL 补抓，OCR 结构化 json(7/16) 09:31 才落地，早报 9:15 已读 08:54 的 7/15 旧批次。
- **关键澄清**：7/16 数据早已存在（09:31 补抓写出 `yupen_2026-07-16_sector_rotation.json`，data_date=2026-07-16）；旧早报 md 第3/56行写死「15号(STALE)」是跑批时机问题，非未抓。
- **采用方案 B 微调（用户拍板「按推荐来」）**：
  1. 鱼盆提取自动化 rrule 改 `BYHOUR=8;BYMINUTE=50`（提前15分钟，给 OCR 留缓冲，确保 9:15 早报前最新落地）
  2. 早报 prompt 新增 **Step 2.5 鱼盆时效自检**：`read_yupen_data.py --freshness-only` 取 data_date，若 < 昨日则板块段强制标[STALE] + **不自动烧 OCR 补抓**（视觉模型成本高，改人工/补跑确认）
- **今日修正重发**：改早报 md 第3/56/62行（15→16，去错误STALE声明），`push_morning_report.py --date 20260717` 重发修正卡片（新 docx `XcGYdK87BoW8Gpx8bLhcmu5onbf`，鱼盆现=7/16 最新）。群内旧错卡片(7/15)未撤回（飞书机器人不可API撤回），以新卡片覆盖。

## 🐟【鱼盆】失败告警加固（13:01，automation-1783472286775 v4→v5）
- 用户拍板"按你建议来"(选项①)：给鱼盆提取加**失败告警**。
- 根因：`fetch_yupen_rss.py` 抓不到时只写 `no_data` 占位 + `read_yupen` 报 `freshness=none`，**静默不推送**，早报会用旧数据而无人知晓。
- v5 逻辑：STEP0 抓前记基线 `BASE_DATE`；STEP1 抓后 `find` 检测 `no_data`占位/`freshness=none` → 推`[STALE] 鱼盆提取失败`并 exit0；软失败(DATA_DATE==BASE_DATE) → 推`[STALE] 未推进`但继续；正常路径不变。
- 验证：临时 `yupen_20260717_no_data.json` 模拟 → 正确触发告警；清理无残留。
- 关键坑：`--freshness-only` JSON **无 `status` 字段**，硬失败须用 `freshness=none` 判定。

## 🔧 飞书推送全量卡片化迁移（13:40–14:00，计划1+2+3+4）
- **目标**：把今天验证过的 `push_card.py` 中台铺到全部推送，统一"语义配色+分区"卡片风格。
- **策略（零 prompt 改动覆盖 23 个）**：让两个薄壳 `push_feishu.sh` + `notify_center.py` 内部委托 `push_card.py`，所有调它们的自动化自动升级；仅对 3 个直发 `lark-cli` 的单独改。
- **改造清单**：
  1. `push_feishu.sh` v2：委托 `push_card.py`（`--title`+`--section "" "$BODY"`，`PUSH_LEVEL` 环境变量控配色，默认 info）。
  2. `notify_center.py` v2：新增 `--level` 显式配色 + 按 event-type 关键词推断（失败/告警/止损→alert，警告/降级/滞后→warning，成功/完成→success，兜底 info）；降级路径改调 `push_card`（**彻底废弃 `--text` 丢格式**）。
  3. 晚报 `1782817769722`：删旧"禁止 interactive card"铁律，改调 `push_card.py --level info`（保留 11 段模板不变）。
  4. 双副本审计 `1783671485008`：`lark-cli --markdown` → `push_card.py --level warning`。
  5. `cost_dashboard_feishu.py`：`send_feishu_message` 改调 `push_card.py`（原 `--markdown` 直发废弃，429/兜底在 push_card 内）。
  6. `earnings_calendar.py`：`push_to_feishu` 改调 `push_card.py --level info`。
- **保留直发**：飞书通道自检 `1784084428353` 必须 raw `lark-cli --markdown`（连通性测试需真实 message_id）。
- **僵尸未动**：`src/claw/feeds/wx_publisher.py`、`_push_morning_20260717.py`、`_push_wx_card.py` 仍有 `--text`，但无活跃调用方（已被 push_morning_report.py 取代），按"删前确认"规则不删。
- **验证**：① `push_card.build_card` 单元（7元素/蓝头/合法JSON）✅ ② `push_feishu.sh` 参数装配直达 push_card ✅（真实 lark-cli 被调，仅因测试 chat 无效报 230001，证明链路通）③ `notify_center` 委托链 + `infer_level` 4 类映射正确（用假 push_feishu.sh 截获 argv+env）✅ ④ `cost_dashboard_feishu.py`/`earnings_calendar.py` py_compile + import ✅。
- **终态**：30 活跃自动化，23 经卡片中台，1 直发(通道自检)，6 不推送；活跃链 `--text` 零残留。

## 🧹 技术债清理（15:06，按用户「清掉①」「停②」）
- **① 僵尸脚本清理（已办）**：
  - 核查:git 暂存区 `D _push_morning_20260717.py` / `D _push_wx_card.py` 实际**磁盘已不存在**；仅 `src/claw/feeds/wx_publisher.py`(2000B)真实存在。
  - 调用方:全仓 **0 直接/间接 import**(feeds 包 `__init__` 不引、无 `import wx_publisher`),无活跃自动化引用 → 安全删。
  - 铁律执行「先复制→验证→再删源」:
    - 副本 `/Volumes/ZHITAI/WorkBuddy_claw_archive/zombie_backup_20260717/wx_publisher.py`(sha256 `05c9f9...71c1` 与源一致、可 py_compile ✅)
    - 已 `rm` 源 → 副本仍在 ✅,git 标记 `D`
- **② 旧链 1783310235388 核查（无需新操作）**：
  - 用户要求"先 diff 再 PAUSE",核实发现**该链 7/14 起已是 PUSED**(deleted_at 已置),最后运行 7/10,新双链(助理实盘 `1784039316540` ACTIVE + 投顾策略 `1784039339114` ACTIVE,今日 14:17 仍跑)完全覆盖。
  - 结论:无重复推送风险,**不重复 PAUSE**。
  - 遗留干净度选项:可彻底 soft-delete(置 DELETED,与已删 1780759171289/178079008345/1781220962599 一致)防误 resume —— 属删除动作,待用户确认。
- **终态**:僵尸文件清零(仅 1 个真存在已清);旧链维持 PUSED。

## 🐞 晚报卡片占位符 bug 修复（16:05，用户「修改」）
- **现象**：用户截图晚报卡片标题/正文显示成 `title`/`body` 占位符，无真实内容。
- **根因**：晚报 prompt v5 的 Step 2 写 `--section "" ""` 空壳，LLM 把"下方精华内容"当 bash 续行文本，**没拼进 `--section` 第二参数** → push_card 收到 `[("", "")]` → 空卡片。早报正常是因 `push_morning_report.py` 在 Python 内解析 md 后调 `build_card`，不走 bash 占位符。
- **修复双管**：
  1. 晚报 prompt `automation-1782817769722` v5→**v6**：Step 2 改为「①Write 工具生成真实精华 md 文件 → ②`CARD_BODY=$(cat 文件)` 单条 `--section "" "$CARD_BODY"` 传入」，title 用 `$(date +%Y-%m-%d)（$(date +%A)）` 真实值；卡片精华结构里的 `{...}` 明确"必须替换真实数据"。
  2. `push_card.py` 加**占位符/空内容防护**：`_looks_like_placeholder()` 检测花括号模板变量（`{日期}`/`{周几}`/`{code}`/`{x}`等）+ 孤立单词（`title`/`body`）；空 body 拦截；**空 title `""` 合法放行**（晚报设计用空标题整块）。main() 在发送前校验，命中则 exit 2 拒绝并报错提示。
- **验证**：5 场景全过（真实内容放行走到发送 / 占位符 title·body·{code}·空 body·孤立 title 单词均拒绝 exit 2）。
- **关键坑**：zsh 里 `$VAR` 含 `\n` 会被 shell 拆参，测试须用 Python 直接构造 `sys.argv` 绕过；真实自动化用 `$(cat file)` + 双引号包裹无此问题。
- 今日晚报 15:35 已错过（发了空卡片），下次交易日下午 15:35 自动生效；如需今日补发需手动触发。

## 📊 晚报补发（16:10，用户「补一条正确的」）
- 今日 15:35 晚报因 v5 占位符 bug 发了空卡片；用户要求补正确版 → 手动触发。
- 数据：大盘暴跌 上证-3.05%/深证-5.40%/创业-7.15%；实盘双仓实时价 长电85.49(-12.9%)/华天18.72(-21.9%)，**均击穿-8%止损线🔴**。
- 先拉实时价（qt.gtimg 直拉 85.49/18.72 + calc_rsi RSI 46.3/43.5），**未用 portfolio.json 旧 current_price(92.46/23.11) 冒充**（守持仓同步铁律）。
- 产出：①`output/wx_reports/20260717_evening.md`（11段完整）②卡片精华 md → `push_card.py --section "" "$CARD_BODY"` 推送（v6 新路径）。
- **验收**：push_card 防护未误杀真实内容，卡片正常发出 `message_id=om_x100b6aaa7d7714a0dfa8a52454d62fb` ✅。证明 v6 prompt + 防护双修闭环。
- ⚠️ 明日开盘操作依据：长电600584/华天002185 必须卖出，回收≈¥31,263 转现金，严禁补仓（已写入晚报「明日备忘」）。
- 注：今日成本用 09:16 成本监控值 ¥22.70（cost_tracker.py today 子命令名错，已知，非本次引入）。

## 📊 晚报推送长效化（16:21，用户选 B）
- **目标**：晚报卡片也要「📄 完整报告」可点按钮，和早报对齐（早报靠 `push_morning_report.py` 建 docx+按钮）。
- **新建 `scripts/push_evening_report.py`**（复用早报 `_create_feishu_doc()` 逻辑）：读 `output/wx_reports/{today}_evening.md` → `lark-cli docs +create` 建文档取 url → 解析 md 标题层级成 sections（风险/止损段含🔴→alert 红头）→ `push_card.py` 发卡片，按钮指真实 docx url。支持 `--date` 覆盖。
- **晚报 prompt v6→v7**：Step 2 从"手写精华+push_card"改为直接调 `push_evening_report.py`（完整 md 是权威源，脚本自动解析，消灭手写占位符风险）。PHASE 1 新增「实时价直拉 qt.gtimg」步骤，明确禁 portfolio.json 旧 current_price 冒充（守持仓同步铁律）。
- **验证（用今日 20260717_evening.md 实测）**：✅ 文档建 `https://tcnuc7gyxhyd.feishu.cn/docx/PH0mdgqCuoJDaFxRXWYcfibannY` ✅ 卡片 `om_x100b6aaa2d33fcb8c2317d3bafb8b1b` level=**alert红头**(自动识别🔴止损) 10区块 ✅ 按钮就位。覆盖了 16:10 补发的无按钮蓝头旧卡。
- **终态**：早报/晚报现在统一走 `*_report.py` 自治建文档+按钮模式；push_card.py 是底层中台（含占位符防护）。

## 📝 周报卡片占位符修复 + 补发（17:05，用户截图 W29 周报 title/content 占位）
- **现象**：周五 17:00 周报 `automation-1780632815958` 卡片同样显示 `title`/`content` 占位符，无真实内容。
- **根因**：周报 prompt v5 只写「推送飞书群」，未指定具体调用方式，LLM 自己编了 `--section "title" "content"` 空壳（与晚报同病）。
- **修复**:
  1. 新建 `scripts/push_weekly_report.py`（复用晚报/早报 `push_*_report.py` 模式）：读 `output/wx_reports/{today}_weekly.md` → 建 docx 取真实 url → 解析 md 标题层级 → `push_card.py` 发卡片，按钮指向真实 docx url；异常/成功率<90% 段自动升级 warning 橙头。
  2. 周报 prompt `automation-1780632815958` v5→**v6**：明确 Step 1 写完整报告到 `output/wx_reports/${TODAY8}_weekly.md`；Step 2 严格调 `$PYTHON $SCRIPTS/push_weekly_report.py`；禁手写 lark-cli / push_card 占位符。
- **补发**：把已存在的 `output/weekly_review_2026-07-17.md` 复制到标准路径 `output/wx_reports/20260717_weekly.md`（sha256 一致 ✅），运行 `push_weekly_report.py --date 20260717` → 建 docx `https://tcnuc7gyxhyd.feishu.cn/docx/P9dndrayCorXGnxxd5KcI4tfnTc` + 卡片 `message_id=om_x100b6aab41b330a4c16dede0e1a625a`（level=warning，8区块）✅。
- **下周五 17:00** 自动按 v6 链路跑，不会再出占位符。

## 🗑️ 旧链彻底删除确认（15:16，用户「删除」）
- 用户拍板删旧链 `1783310235388`（防误 resume 重复推送）。
- 核实：`automation_update delete` 回报 "already deleted or does not exist" → 印证 7/14 起已 soft-delete（deleted_at=1784181555413 已置）。
- 终态：未删除视图 0 条（彻底离场 ✅），与已删组 1780759171289/1781220962599 口径一致。178079008345 查无（N/A，疑早期已清）。
- 注：本次"删除"实际为幂等确认，无新写库动作；副本不碰（无文件副本，仅 DB 标记）。

## 🛡️ 自动补跑（automation-1783469126245, 16:30）
- **全部正常 → [SILENT]**
- 收盘复盘：✅ 存在（`/tmp/claw_closing_report_2026-07-17.txt` message_id + `evening.md` 3075B）
- 早报：✅ 存在（14KB）
- 持仓监控：✅ 锁文件 `助理实盘监控` + `投顾策略执行` 均存在（grep "monitor" 因中文名返回0，同前次误判模式，非真缺失）
- 无需补推。`done_schedule` 完成（成本估计跳过，锁文件已落盘）。

## 🔍 占位符 bug 全量排查（17:27，用户「检查还有没有别的」）
- **方法**：sqlite 导出 32 个 ACTIVE 自动化全量 prompt → Grep `--section|--text|--markdown|lark-cli|push_feishu` 风险模式 → 逐个读原文判定。
- **发现 2 个同类隐患（裸模板变量直传，晚报 v5 同病）**：
  1. 🔴 **美股监控 `1780615006148` v5**：`push_feishu "美股收盘" "🇺🇸【美股收盘】{date}\n道指: {收盘} {涨跌}%..."` —— `{date}`/`{收盘}`/`{涨跌}%` 全裸变量，LLM 漏替换即发字面量。
  2. 🟡 **双副本审计 `1783671485008` v3.1**：`push_card.py --section "" "{发现详情}"` —— `{发现详情}` 变量需 LLM 替换（走 push_card 有防护，风险低但丑）。
- **其余 30 个安全**：23 个经 `push_feishu.sh` v2 委托 push_card（传入已是替换后真实文案）；6 个不推送；1 个（飞书通道自检 `1784084428353`）**故意** raw `lark-cli --markdown` 测连通性（无变量，零风险，维持）。
- **修复**：
  1. 美股监控 v5→**v6**：Step 改为「Write 写 `output/wx_reports/${TODAY8}_usstock.md`（所有 `{...}` 替换真实值）→ `CARD_BODY=$(cat 文件)` → `push_card.py --section "" "$CARD_BODY"`」，与早/晚报对齐。验证：Python 构造等价调用 RETURN 1（放行，仅测试 chat 230001），防护不误杀 ✅。
  2. 双副本审计 v3.1→**v3.2**：`{发现详情}` 改为「先 Write 真实审计结果 md → cat 传入」，加占位符硬约束说明。
- **终态**：全部活跃推送链「占位符 bug」清零（早/晚/周/美股/审计均走 md→push_card 或 v2 委托）。push_card 占位符防护为兜底闸。

## 🔍 今日变更审计（17:31，用户「审计下今天的变更」）
- **方法**：git status（初看 37 文件变动，多数为历史未提交累积）→ 用 mtime 精确筛 7/17 当日改文件 → 交叉核验 DB prompt 版本号 + py_compile + bash -n + JSON 合法性。
- **澄清 2 个 git 误报**：
  1. `D .workbuddy/scripts/cost_monitor.py` 是 commit `8521339`(7/11 前)"双副本废弃"已删，磁盘 `scripts/cost_monitor.py` 完好 → 非今日删、非误删 ✅（记忆"不能删"指防未来误删 scripts/ 那份）。
  2. `M .workbuddy/data/user/portfolio.json` mtime=7/15 19:53 → 今日未动，虚惊 ✅。
- **今日真实改动清单（脚本/配置）**：
  - 推送中台：push_card.py(防护) / push_feishu.sh(v2) / notify_center.py(v2) / push_evening_report.py(新) / push_weekly_report.py(新) / push_morning_report.py(11:46)
  - 委托改造：cost_dashboard_feishu.py / earnings_calendar.py / schedule_utils.py(6h锁+stats)
  - 数据：data/simulation/portfolio.json(initial_capital=50000+实时价 17:20) / fetch_yupen_rss.py(--article-id)
  - 僵尸清理：src/claw/feeds/wx_publisher.py(已删 15:06)
- **DB prompt 版本核验全一致**：美股 v6 / 周报 v6 / 晚报 v7 / 双副本 v3.2 / 早报(调 push_morning_report.py) ✅
- **编译/语法/JSON 全绿**：9 个 py 文件 py_compile 通过 + push_feishu.sh bash -n OK + portfolio.json 合法 ✅
- **结论**：今日变更自洽、无残留破损、无未授权数据改动。建议下一步 git commit 把 7/17 工作落库（当前全在 working tree 未提交）。

## 📊 工程质量报告（automation-1782002834355, 18:00）
- 门禁：交易日✅ + 调度稳态✅ → 执行
- ruff：初始 65 项 → `--fix` 自动修 48 → 残 17
  - 🔧 **主动修复 2 处真实 NameError bug**（非 --fix 可修）：
    - `scripts/earnings_calendar.py`：`push_to_feishu()` 缺 `import os`（5 处引用必崩）→ 已补（与今日卡片化迁移一致）
    - `src/claw/feeds/wx_collector.py`：`_fetch_today_via_api()` 本地订阅补全分支用 `is_morning` 但未传参 → 已补形参 `is_morning=False`
  - W293 空行空白 5 处（`--unsafe-fixes --select W293` 精确清理，零风险）
  - 最终残留 7 项（样式/软安全，非安全可自动修）：SIM103×1 S314×1 S110×2 SIM105×1 PLC0206×1 PLW2901×1
- F401：All checks passed ✅ / pyc+__pycache__：已清理
- bandit 直跑（security_scanner.py 仍缺失）：**0 高危** / 1 中危 / 193 低危 / 11423 行 → 无🚨（中危=XML解析建议 defusedxml；对比 7/16 的 3 高危弱MD5 已清零）
- 推送：已发修复记录+待处理清单（dedupe-key 工程质量报告-2026-07-17, level=info）✅
- 收尾：schedule done ✅ / cost_tracker 跳过（无估算配置）

## 18:08 风险巡检(automation-1782035436209) v3→v4：加开盘检测/非交易日硬拦截
- 用户点出漏洞：周六补跑会读 portfolio.json 看到双仓 -12.88%/-21.95% 击穿止损→推"止损提醒"噪声（周末券商冻结、非真下单）。
- 改造：GATE 0 用 `python3 scripts/is_trading_day.py` 显式硬判定（替代依赖 `$CLAW` 的 `check_trading_day`，防沙箱失锁漏拦）；加 09:30 后开盘守卫跳过。
- 验证：周六(07-18)exit=1✅ / 交易日(07-17)exit=0✅ / 节假日(01-01)exit=1✅。
- 结论：非交易日/已开盘→静默exit 0，绝不读portfolio推止损提醒；双仓真实止损触发仍等下周一(07-20)盘中链。

## 18:40 同类漏洞延伸治理：助理全主板选股扫描(automation-1784013251841) v1→v2
- 风险巡检 v4 后顺手扫 23 个交易/系统类 ACTIVE 自动化。
- 唯独选股扫描 prompt 全文无交易日判定（仅 BYDAY 拦周末，调休周六/节假日会误跑推买入候选）→ 同款真漏洞，已修 v2（GATE 0 加 is_trading_day.py 硬判 + 收盘<15:00 守卫）。
- 其余 21 个：周末固定报告类(周报/股票池体检/下周前瞻/资产配置周报/美股/财报日历)设计上本就不读portfolio推交易提醒，安全；系统维护类(记忆/健康/备份/成本看板)纯巡检不读portfolio，安全；其余交易类(早报/晚报/双盘中链/智能选股/午间选股/补跑/鱼盆/QTS日评)已含 is_trading_day 硬判，扎实。
- 验证：周一(07-20)exit=0✅ / 周六(07-18)exit=1✅。
- 结论：交易相关自动化开盘/交易日守卫已全量闭环。

## 19:35 修复晚报第八段 QTS 11策略未消费 bug
- 用户纠正：约定是"QTS 11策略放进晚报第八段"，但我之前只改了模板(evening_report_template.md 160-172行)没在 assembler 实现消费逻辑 → 今晚晚报第八段显示"今日数据暂缺"。
- 根因：`wx_assembler.py:build_evening_report()` 第七段后直接收尾，完全没有读 `/tmp/qts_daily_brief.json` 的代码（模板占位≠实现）。
- 修复：在第七段后插入第八段消费逻辑（读 brief → 校验 report_date==今日才消费，否则丢弃显STALE → 填回测概况+Top5表+WF趋势解读）。
- 验证：单元验证输出正确（33286次回测/WF通过0/全过拟合标注）；ruff 仅剩729行PLW2901为历史遗留AI复盘代码（不在本次范围，未动）；S108(/tmp路径)加noqa抑制（跨项目桥接约定路径）。
- 结论：明晚起晚报第八段将自动消费QTS日评；今晚已生成的晚报因16:01已有brief但16:09跑时assembler无逻辑→未填入，属历史性一次缺失。

## 19:53 补发今晚晚报 + 第八段加股票名称映射
- 用户要求：①补发今晚晚报(第八段已修) ②代码映射股票名称(检索记忆找映射表)。
- 映射表定位：Claw 侧 `.workbuddy/scripts/astock_code_name.json`(5528只,键=纯数字代码如600584)；QTS `shared/stock_name.py` 同源但禁跨项目import。
- assembler 加 `_load_stock_names()`+`resolve_stock_name(ts_code)`：剥.SH/.SZ后缀→查纯数字键→返回`名称(代码)`，查不到显纯代码。
- 第八段表格`标的`列改 `resolve_stock_name(ts_code)` 渲染（灵康药业/和远气体/时空科技/甘咨询/厦门钨业均正确解析）。
- 补发流程：16:09 md 第八段仍"暂缺"(旧逻辑)→ 用最新第八段(含名称)替换 md 七八段区间 → `push_evening_report.py --date 20260717` 推卡片。
- 推送：✅ message_id `om_x100b6a95cb2060a0c3d53f6f52f61ca`(level=alert，双仓止损🔴)；飞书文档已重建。
- ruff：新增2处自修(PLW0602 global赋值/PLC0207 split→partition)，剩729/761行PLW2901为历史AI复盘代码(不在范围未动)。
- 结论：晚报第八段消费QTS 11策略+股票名称映射 双功能闭环，明晚起自动化直接生效。
