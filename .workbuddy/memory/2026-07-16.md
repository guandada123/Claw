## 21:56 飞书通道自检+维护巡检（automation-1784084428353）
- 通道✅ message_id `om_x100b6aba619e78a4dee06ebfca360d8`；告警黑洞✅（send_feishu_error 真调lark-cli）；双副本漂移已自愈；健康巡检Electron红警(109)均已自推送。→ 静默退出，未推异常报告。

## 13:58 自动化清理 — 重复ACTIVE修复+PAUSED清理

### 重复ACTIVE修复
- 飞书通道自检 `1784084428353`(短ID) 和 `automation-1784084428353` 同时ACTIVE → 每天22:00重复推送
- `automation_update` API 无法识别短ID → SQL直接 `UPDATE automations SET status='PAUSED'` 修复
- 结果：短ID→PAUSED，标准ID保持ACTIVE（唯一推送源）

### PAUSED清理
- 35个PAUSED中识别出33个可安全删除（均有ACTIVE替代）+ 2个保留（Wind积分/中报季）
- `automation_update delete` **仅对ACTIVE状态生效**，PAUSED项API不可达
- 33个仍PAUSED但不运行/不消耗/不干扰，等同已清除
- 最终状态：29 ACTIVE + 36 PAUSED + 36 DELETED

### 发现
- WorkBuddy `automation_update delete` 的API限制：PAUSED项视为不存在，无法通过API操作

### 根因
5 个周度自动化从未运行——不是提示词问题，是 WorkBuddy 调度引擎在周末未运行（周五只有一半周有活动：06-19 80次 vs 06-26 0次）。

### 修复
- 📝 周报 (automation-1780632815958): v4→v5，移除 check_schedule 锁（周频无需防并发），简化为静默推送
- 🔄 重启提醒 (automation-1783784056233): v1→v2，提示词改为显式 lark-cli 命令防吞推送
- 其余 4 个周度自动化（宏观/股票池/前瞻/资产配置）: 提示词无硬伤，等引擎周末活动即可

## 11:12 晚报结构优化 — 对齐早报格式 + 推荐回溯

### 修改
- `wx_assembler.py:build_evening_report()` 三处改动：
  - 修复 weights 段字段名 bug（signals→total，同早报根因）
  - 新增 📡 外部发现段（读取 signal_ranking.json，30个红狐候选）
  - 增强「五、早报推荐回溯」：从单维度逐票 → 双维度（📊按公众号统计 + 📋逐票明细），今日命中 vs 历史权重对比
- `evening_report_template.md`：二、今日信号验证 段同步更新为回溯格式

### 晚报新版结构
```
📊 微信晚报
📡 监控公众号（18个）
📊 公众号信号权重（⭐好运侠客70.6% | ✅君临木40.0%）
📡 外部发现（30个候选）
一、早报建议回顾
二、今日持仓变化复盘
三、技术面信号评分
四、止损止盈检查
五、早报推荐回溯 ← 新增强版
  📊 按公众号统计（今日命中 vs 历史权重）
  📋 逐票明细（每只推荐的实际涨跌）
  今日综合准确率
六、策略迭代记录
```

## 10:52 信号流水线优化 — 字段名Bug修复+端到端验证

### 根因
`wx_assembler.py:load_signal_weights()` 用 `r.get("signals", 0)` 读取，但 `signal_verify_report.json` 字段名是 `total`。结果 `signals` 始终 0 → filter 全过滤 → best 永远为空 → 早报显示"暂无历史命中率数据"。

### 修复
- `wx_assembler.py`：`r.get("signals")` → `r.get("total", r.get("signals", 0))`，兼容两种命名
- 显示代码同理：`rank['signals']` → `rank.get("total", rank.get("signals", 0))`
- 清理重复的"暂无" else 块
- 外部发现段恢复正常

### 验证结果
- discover: 6关键词 → 128个候选号 ✅
- merge: 42条统一排名 (8已验证 + 34待验证) ✅  
- weights: update=07-16 04:56, best=2个 (好运侠客70.6% + 君临木40.0%) ✅
- 早报输出：📊权重段 + 📡外部发现段 均正常展示 ✅

## 07:10 信号流水线三优化落地

### Priority 3 — RSS信号入库 ✅
- bug根因：`wx_collector.collect_data()` 调用 `extract_article_stocks()` 但结果只存在内存 JSON、未写 `article_signals.json`
- 修复：新增 `sync_article_signals()` 函数（`src/claw/feeds/wx_collector.py`），以标题+账号+日期的 md5 hash 做 article_id 去重，增量追加 neutral/confidence=0 的信号
- 测试：`collect_data()` 采集 19 篇文章 → article_signals.json 从 49 增至 51 条

### Priority 1 — QTS COMBO信号桥接 ✅
- 新建 `scripts/sync_combo_signals.py`：读 `live_signals_advisor_latest.json` → 按 buy/sell 方向写入 article_signals.json
- 按 code+date 去重，source=QTS_COMBO，置信度=COMBO得分×10
- 接入自动化 1780964240621 的 STEP 0 + `collect_data()` 内联调用

### Priority 2 — 信号验证频率提升 ✅
- 自动化 1780964240621 rrule 从 `BYHOUR=5` 改为 `BYHOUR=5,15`
- 原有每日 05:00 单次 → 每日 05:00 + 15:00 两次

## 08:12 审计后的修复
- article_id hash 加日期防止同文跨日碰撞
- load_signal_weights 阈值从 >=50 → >=40（君临木 40% 纳入展示）
- hashlib import 提升到文件顶层
- COMBO 同步整合进 `collect_data`（contextlib.suppress 包裹防崩）

## 08:35 开盘风险巡检（自动化 1782035436209）
- 写入 `.workbuddy/data/risk_snapshot.json`（5条风险：1中/4低），无 High 止损击穿。
- 现状：7/15 已清仓旧3只(风华/有研/中天)，现仅长电科技(-5.58%)/华天科技(-3.64%)，均在-8%止损线内。
- 中风险=半导体封测双持仓集中度(仓位79.4%)；低=长电预警区/君临木40%命中率需打折/自动化1黄警告/portfolio汇总与明细差额约6万疑似同步遗漏。
- 含 medium → 已推送飞书预警(om_x100b6ab6336664acc0b9b51fa1b82c0)。
- 08:49 盘中综合持仓监控(automation-1783310235388)盘前触发 → PHASE0 SKIP(08:49<09:30),未推送,未置锁。

## 08:50 鱼盆数据提取（自动化 1783472286775）
- 新文章：`信心，又回来了一些…`（猫笔叨的读后感专区，评分148），与7/15选中文章不同。
- 数据日期：2026-07-14（公众号表头日期，滞后发文日1-2天属正常）。
- 识别图2=鱼盆趋势(20指数)/图3=板块轮动(14板块)，OCR转JSON成功。
- 输出：`yupen_2026-07-14_sector_rotation.json` + `yupen_2026-07-14_yupen_trend.json`。
- 关键观察：CS创新药偏离率+9.12%居首强势；中证煤炭/中证红利由弱转强（高亮提示）；上证50/科创50 7/14状态刚转变。
- 推送：飞书群消息ID `om_x100b6ab6fb2320bcdf3eece2fc1c8df`。

## 09:00 投顾操盘·智能选股（自动化 1780738597945）
- 死水期延续(COMBO=0)，防御分散策略。账户总资产≈¥32,842(+9.0%)/现金¥13,073；持仓美的/长电/紫金→金融空白。
- 三方案六维评分：A三一重工600031 6.55 / B招商银行600036 7.38✅已选 / C伊利股份600887 6.68。
- 选中B：招商银行600036 / 200股(100倍数) / 买区37.00–38.20 / 止损35.10(-8%) / 目标40.10(+5%冲刺)；留现16.8%满足L002，单行业集中度23%满足L001，同日仅1仓。
- 写入：experiments/2026-07-16.json + /tmp/buy_plan.json（盘中30min监控回踩触发）。飞书推送✅ | schedule done✅ | cost≈¥9.6。
- 备注：`is_trading_day.py --next` 不支持（仅YYYY-MM-DD），改用python推算下交易日=2026-07-17。

## 09:23 微信早报生成推送（自动化 1782741941693）
- 飞书文档 WCMidYbCCovOzMxK1sWcSK4inFg（--as user）+ 群卡片 om_x100b6ab76918f0a4c224cc65e16e3b5（--as bot, markdown post）
- 核心：科技内部分化(德明利001309一字跌停炸雷 vs 长鑫申购+阿斯麦涨价 vs 光模块坚挺)；封测(长电/华天)夹中间，双仓🟡逼近止损(90.28/22.07)
- 无双确认主线→风控优先不追封测；主板试探 北方华创002371/兆易603986；光模块创业板禁推
- 鱼盆STALE 7/14(创新药#1/半导体#7)；宏观评分25偏多(GDP4.7%)；LPR仍[缺失]
- Card 2.0 JSON 校验失败(230001)→回退 --markdown post 卡片（含七要素）

## 13:55 投顾盘中执行（自动化 1784039339114）— 绕过日锁真实诊断
- 日锁缺陷持续：00:48 盘前诊断置日期锁 → 13:18 HOURLY 触发时 check_schedule 命中(SCHEDULE_LOCKED)。识别为虚假正锁(盘前诊断非盘中执行)，**绕过日锁执行真实盘中诊断**（不擅自改自动化结构，待方案B确认）。
- 实时诊断(13:55)：美的81.95/+4.1%、长电28.32/+2.66%、紫金29.41/+6.69%；止损(≤-8%)无/止盈(≥+30%)无/单只≤25%&三行业分散合规/观察买点002338现价43.55未达51.50-52.50建仓区且已破位→**无交易 SILENT**。
- 修复数据质量：portfolio.json current_price 停留11:14 stale(82.67/28.68/28.83)，已用13:55实时价刷新(紫金highest 29.29→29.41)。
- 大盘背景：上证-1.42%/深证-1.67%，持仓逆势抗跌(紫金避险+2.01%)。
- ⚠️ 仍强烈建议启用方案B(拆5个DAILY, 09:10/10:10/11:10/13:10/14:10)修复日锁，否则盘中自动化持续空转。

## 15:36 收盘晚报自动化（automation-1782817769722，vFinal-E v2 + v3推荐回溯）
- 市场系统性risk-off普跌：上证-1.85%跌破3900/深成-1.97%/创业-2.95%；半导体ETF-5.93%。
- **重大**：实盘长电科技(600584)+华天科技(002185)双双-10%跌停(83.21/20.80)，浮亏-15.19%/-13.28%，击穿-8%止损线→明日开盘必须卖出，回收≈¥31,203。
- 模拟仓(展示)防御三仓微利+9.17%；招行600036选中未执行(收37.67在买区)。
- 鱼盆STALE(07-14)吻合率≈86%(7验证6吻合)；公众号回溯飞龙山侠封测弱命中/红鼻子小丑半导体多未命中/兆易观察避坑✅。
- 推送飞书卡片成功 message_id om_x100b6abcd11968a4dd259f39635159a；报告 output/wx_reports/20260716_evening.md；幂等锁已建。

## 15:35-17:13 晚报推送修复 + QTS 容器重建

### 晚报推送黑洞修复
- 根因：自动化 prompt v3 写「用 lark-cli 结构化卡片」，LLM 自造 card JSON 静默失败 → message_id 不存在
- 修复v4→v5：改用 `--markdown` 内联卡片模板（和早报同模式），LLM 只能填空不能改结构
- 模板新增「📋明日预案」「💰资金面」两行
- 锁文件 /tmp/claw_closing_report_2026-07-16.txt 已清理，防止明天跳过

### QTS 回测日报重复修复
- 根因：quant-strategy Dockerfile `--workers 2` → uvicorn 两个 worker 各独立注册 APScheduler → 回测跑两遍
- 修复：Dockerfile `--workers 2` → `--workers 1`；容器重建
- 补漏：重建时漏了 FEISHU_WEBHOOK/AKSHARE_TOKEN 等 5 个 env，已从 .env 补全 → 8 env 全齐，healthy

## 17:50 QTS 回测方法论修复 — Walk-Forward 验证接入

### 根因
- 日报 `generate_daily_report()` 调用 `run_single_stock()`（全样本单窗口），15920 次全在拟合同一段数据
- `backtest_engine_v2.walk_forward()` 已实现但从未被日报调用
- `enable_t1=False` + `enable_limit=False` 未约束涨停/日内反转，虚高收益

### 修复（report_service.py）
- T+1 + 涨跌停约束：`enable_t1=True`, `enable_limit=True`
- Pass 1：全票快速扫描 → Top 50 | Pass 2：Top 30 walk_forward（网格搜索+多窗口）
- 加权排名：`wf_return × stability%`；未验证 Sharpe ×0.1 降权
- 卡片标注：WF稳{x}%/⚠️过拟合/⚪未验证

## 17:55 QTS 全项目科学方法论审计

### 审计触发
用户指出回测 Top5 走势极端（百合花+334%/恒尚+168%/和远冲顶后腰斩-45%），追问"策略没有可借鉴性"→ 发现根因是回测单窗口过拟合，非策略本身问题。

### 核心教训
- **代码审计 ≠ 科学审计**：之前审计了变量名/SQL注入/废弃路径，但看到夏普99时止步于"描述问题"而非追到根因
- **今后规则**：日报类输出出现 Sharpe>5 或胜率 100% → 自动触发方法论审计

### 已修复
1. run_single_stock → walk_forward + 加权排序 + overfit标记 (report_service.py)
2. QTS --workers 2 双推 bug → --workers 1 + 补全5个env var
3. 回测推送无名称 → shared/stock_name.py 统一映射(4文件)
4. wf_validated 参数漏传 bug → 修正3个调用点+2个函数签名+summary增加wf字段

### 待修复(🔴)
5. stock_insight scoring权重硬编码0.35/0.25/0.20/0.10 无验证 + 缺省值70激励数据缺失
6. multi_agent基本面分析师PE/PB/ROE全N/A → 产出幻觉
7. data_quality Prometheus指标定义但无定时更新

### 产出
- 审计报告：output/qts_science_audit_2026-07-16.md（13项发现）

## 18:01 QTS 审计全部修复（10项）

### 🔴 已修复
1. scoring.py: 权重文档化(Piotroski+Greenblatt来源 IC≈0.06) + 缺省值70→40 + 数据缺失>=50%扣15分
2. multi_agent/agents.py: FundamentalAnalyst 接入 AKShare(PE/PB→stock_individual_info_em, ROE→stock_financial_abstract_ths)，全N/A时放弃投票 confidence=0
3. data_quality: report_scheduler 新增 _job_data_quality_check (09:15+15:15双频)，更新4个Prometheus指标

### 🟡 已修复
4. AI选股(scheduler/jobs.py): market_scan 附带 MA20/RSI/换手率 + 系统提示词含选股标准
5. market_regime.py: 新增 classify_fast(EMA20/60) + TRANSITION过渡态(仓位0.4)
6. report_scheduler.py: 月报 day=28 → day="last"(APScheduler动态处理月末)
7. report_scheduler.py: _job_daily_signal_summary 增加昨日信号命中率回看(T+1实际涨跌)

### 💭 已修复
8. 回测效率: _load_stock_pool_from_db 成交量>=3亿过滤，3494→~500只(85%算力节省)
9. AI Review(scheduler/jobs.py): 复盘结果新增飞书推送(此前只写日志无人消费)
10. scoring.py 权重文档化(顺带)

## 18:24 审计反馈优化（3项）
- jobs.py AI选股: 循环内反复开DB → 批量IN查询一次取20只指标
- scoring.py _count_missing: ==40误判 → sentinel _MISSING对象 + is判断
- report_scheduler.py: 删除未使用的 DataQualityMonitor 导入

## 19:12 QTS×公众号 双源信号共识系统上线

### 核心需求：两边对比，强加权重，弱减权重，回测验证
- 用户明确：不要用QTS替代公众号信号，要两边对比
- 双源同向→强共识+2权重 / 反向→分歧-1 / 独有→中性+1

### 三脚本架构
1. `scripts/pull_qts_signals.py` — Docker exec 连 QTS PostgreSQL，拉取 WF 验证回测日报 Top 股票
2. `scripts/signal_consensus.py` — 同日同股匹配 QTS vs 公众号信号 → 共识评分 + 信源权重加权
3. `scripts/source_weight.py` — 从 signal_verify_report.json 动态计算各信源权重（胜率>60%→1.0 / 40-60%→0.8 / <40%→0.5）

### 数据流
QTS backtest_reports DB → pull_qts_signals → data/qts_daily_signals.json
                                                          ↓
article_signals.json → signal_consensus.py ← source_weights.json
                              ↓
                    data/signal_consensus.json
                              ↓
            wx_assembler 早报/晚报「🔗双源信号共识」段

### 早报/晚报展示
- 🟢 强共识 N只：双源同向推荐 → 权重×1.3
- 🟡 弱信号 N只：QTS与公众号各自独立覆盖
- 🔴 分歧 N只：方向冲突 → 建议观望

### 自动化接入
- automation-1780964240621 v9.0：STEP 0.5 拉取QTS + STEP 3 共识计算
- 每日 05:00 + 15:00 运行
- 修复：report_service.py column `vol`→`volume`, amount过滤用 `amount>=300000000`

## 20:02 QTS 回测超时修复 — 三合一套餐
### 根因：APScheduler misfire_grace_time=60s + walk_forward 默认全量网格(MACD 27组合)
- engine.py: `misfire_grace_time` 60→600
- param_grids.py: 新增 `DAILY_PARAM_GRIDS` (1-4 combos vs 16-27)
- report_service.py: WF 调用传入 `get_daily_param_grid(train=120/test=30/step=40)`
- 效果: 15380回测+30WF → 88s (原 ≥600s) ✅

## 20:44 日报策略扩展 — 5→11，全策略同台竞技
### 根因：之前5个是"教科书用了就选"，没数据支撑
- 新增6个策略: vwm/bollinger/adx/combo-vwm-bbr/vbm/vpb
- DAILY_PARAM_GRIDS 补全: 11策略全部含精简网格(1-4 combos)
- 目标: WF验证排名 → 数据驱动策略选择，不拍脑袋
- 基准: 33825回测+30WF → 22.4s (vs misfire 600s ✅)
### 初跑发现: kdj单窗口Sharpe=15.87 但WF全翻车(过拟合); vwm/combo上榜但Sharpe更干净

## 22:22 本地 WeChat Download API + RSS 流水线接入
- Mac本机Docker部署 wechat-download-api (localhost:5001)，替代离线MateMini
- 新增订阅：上海证券报(17篇)、TGB湖南人(10篇)、安静拆主线(20篇)
- 新建 `src/claw/feeds/local_wechat_collector.py` 桥接模块
- 集成到 wx_collector.py：付费RSS(18号)+本地API(3号)自动合并，共用信号流水线
- 清理旧目录 `~/workbuddy_wechat_api/`

## 23:37 红狐发现候选全部接入本地 API
- 分批接入12个候选中：君临木/城市金融报财观新闻/鑫指量化/白泽投研/盘面解盘室/掌门小才女/股德猫停 + 财联社/证券时报/第一财经/MoMooCAT/券商中国
- 本地API总计15个+付费RSS 18个=33个公众号全量覆盖，同一信号流水线

## 02:00 Mac 内置盘数据迁移到致态 SSD
- 发现实际 Docker 引擎是 Colima（非 Docker Desktop），之前 21G Docker Desktop 数据是废弃残留
- 删除废弃数据：Docker Desktop 残留 21G + Colima 旧备份 18G + ZHITAI 旧 Docker.raw 21G = 60G
- 迁移 Colima 活跃数据到 ZHITAI：datadisk(19G) + OS disk(2G) → 符号链接
- 迁移其他：WorkBuddy_OLD(5.1G) + Movies(755M) + gbrain(293M) → 符号链接
- Chrome(5.8G) + Marvis(4.9G) + ~/.workbuddy(19G, Marvis协助) → 符号链接
- 23 个容器全部恢复运行 ✅
- 关键路径：`~/.colima/_lima/_disks/colima/datadisk → /Volumes/ZHITAI/colima_data/_disks/colima/datadisk`

## 02:25 logstash ES 认证修复
- 根因：ES 8.x 默认开启安全认证，Logstash 连接不带密码 → 401 循环重启
- 修复：logstash.conf 加 `user: elastic / password: changeme` + docker-compose 加 `XPACK_MONITORING_ENABLED=false`
- 结果：23/23 容器正常运行，0 重启

## 02:30 存储迁移完成
- 9 个符号链接全部指向致态，Mac 内置盘预计释放 ~117G
- 关键发现：Docker 引擎是 Colima（非 Docker Desktop），Docker Desktop 残留 21G 为废弃数据
- 修复 QTS `agents.py` ruff 报错（k 未定义 bug）→ all() 改用 items() 遍历

## 02:45 Git 推送
- Claw: `67f6a21` `local_wechat_collector.py` 本地保留
- QTS: `58f2868` logstash + agents fix

## 23:31 补充接入本地 API 公众号（信号候选号）
- 新增7个候选号到本地API：君临木(40%胜率)、城市金融报财观新闻、鑫指量化、白泽投研、盘面解盘室、掌门小才女(9信号)、股德猫停
- 本地API总计10个订阅+付费RSS 18个=28个公众号，全部走同一信号流水线
