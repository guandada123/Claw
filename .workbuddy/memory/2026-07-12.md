# 2026-07-12（简洁版）

- 周末维护(记忆+空间)自动化运行：MEMORY.md 路径校正(scripts/*→.workbuddy/scripts/* 等) + 技术债记录；空间全低于阈值无清理
- 发现 schedule_utils.py/notify_center.py 在维护任务 v6 中被引用但项目内不存在，已用 automation_health.py + push_feishu.sh 替代执行
- 自动化健康 37个(27/9/1)，严重项为月度任务阈值误报，未自动PAUSED
- 成本告警自动化 v7 执行：今日¥0.095/本月¥75.86 均正常→静默；新增发现 cost_tracker 实际接口为 daily/monthly/top（v7写的 summary/daily-summary 不存在）+ push_feishu.sh 依赖 FEISHU_CHAT_ID 当前环境未设置→超预算推送链路仍断裂
- 综合健康巡检 v6（07:50）：🔴 内存风险(陈旧会话11/Electron16/free pages波动) + 🔴 stock_pool.json 损坏(行311未转义引号)已修复并备份；3个自动化cwds指向已删临时目录(automation_update工具找不到legacy ID，转人工)；已推送告警。坑：source preamble | head 会让zsh subshell丢失export；内存Pages free波动大以进程计数为准
- 🔄 每日重启提醒自动化(08:00)：向飞书群推送重启提醒，消息已成功发送
- 📊 股票池技术体检 v5(09:00)：判定 [NOT SILENT] — portfolio.json 仅记天娱数科，与实盘5只(中天/有研新材/深科技/风华高科/长电)完全脱节(+400%)；已推送持仓确认提醒+实盘快照+下周重点；缺 schedule_utils.py/notify_center.py 仍走 push_feishu.sh 替代
- 💰 成本监控 v8(10:32)：今日¥0.45/本月¥76.22，预估月底¥191 ✅ 预算内→[SILENT]；最高模型占比41.4%(<50%)。修正v7误判：**推送链路实际可用**（lark-cli v1.0.68在PATH，push_feishu()自动注入FEISHU_CHAT_ID）；v7把preamble函数误当缺失的schedule_utils.py/notify_center.py。🔴新发现：`check_schedule/done_schedule`用每日锁，会封死22:00日报+其他时段告警（与"每6小时"频率冲突），本次故意不置锁以保障22:00日报；建议改小时级锁或去锁。本月TOP含`test`¥13.22脏数据待清理。
- 🩺 综合健康巡检 v6(15:42)：✅ 全正常→[SILENT]静默退出。内存风险已解除（陈旧会话4/Electron9，较07:50的11/16、11:47的18/23大幅回落，重启生效）；Docker无退出容器；端口3000/8000/8001/8002=200、8765=404(正常)；磁盘19%；proxy-deepseek+colima存活；34个ACTIVE自动化cwds均合法；portfolio/stock_pool JSON可解析。notify_center.py"缺失"/router导入失败确认为已知false positive（推送走push_feishu.sh正常）。
- 🩺 综合健康巡检 v3.1(19:41)：✅ 全正常→[SILENT]。内存安全(Electron9≤10/WorkBuddy主进程13)；端口/磁盘/代理/portfolio/numpy2.2.6/pandas2.3.3/cwds(26个ACT主均合法)全绿；v3失败告警修正后近24h失败=0。★ **自愈修复**：v3原失败告警SQL引用不存在的`ar.started_at`列+错误`status IN('failed','error')`(实际仅PENDING_REVIEW/IN_PROGRESS)，每次`OperationalError`且**失败告警从未生效**；已修正prompt(v3.1)改用`created_at`(unix毫秒)+`result_success=0`判失败并写回配置。
- 📊 下周前瞻 v2(20:00)：✅ 已推送(om_x100b6a0c57afa0b8b159bbb24c39ed6)。数据缺口：pool_health.json占位(9:00体检未产出)+macro_weekly缺失→用实时行情+央行例会替代。核心：风华高科(000636)已破-8%止损(70.10→60.01,-14.4%)建议周一止损；全池高beta集中，下周主线防御+业绩验证，等7/15中报预告+政治局会议。产出 output/weekly_outlook_2026-07-12.md
