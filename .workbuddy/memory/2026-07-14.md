# 2026-07-14 工作日志（补充）

## 📈📊 方案B：盘中监控链拆分重建（22:22）
- **背景**：用户复盘发现盘中监控 11 项中 8 DELETED + 3 PAUSED，唯一活口 `1783310235388` 因 `:30` 触发+PHASE 0 时间窗错位导致 9:30/10:30/11:30 三次误判 SKIP
- **方案**：重建两个独立自动化，职责拆分：助理只管监测、投顾只管交易

## 🔧 治理：cost_monitor.py 双副本审计与修复（22:46）
- **触发**：当日变更 94 个文件，判定大重构，跑完整 7 步审计
- **发现问题**：cost_monitor.py 三副本 + 逻辑漂移
  - `scripts/cost_monitor.py` (113行, canonical) → 现行编排版
  - `.workbuddy/scripts/cost_monitor.py` (273行, DEPRECATED) → 被 3 条自动化引用
  - `.workbuddy/scripts/cost_monitor_legacy.py` (274行, deprecated) → 零引用
- **修复内容**：
  1. `automation-1781428691860`（AI成本监控）: 绝对路径从 `.workbuddy/scripts/cost_monitor.py` → `scripts/cost_monitor.py`
  2. `automation-1782002178528`（成本日报）: `$SCRIPTS/cost_monitor.py` → `$CLAW/scripts/cost_monitor.py`
  3. `automation-1782002819199`（成本监控6h）: `$PYTHON $SCRIPTS/cost_monitor.py` → `cd $CLAW && $PYTHON scripts/cost_monitor.py`
- **备份**：三份副本已备份到 `/tmp/`（cost_monitor_scripts_backup.py / cost_monitor_dotworkbuddy_backup.py / cost_monitor_legacy_backup.py）
- **状态**：废弃副本在本次会话前已被清除（备份完好），无残留引用，不构成回归风险
- **新建**：
  1. `automation-1784039316540` 📊【盘中】助理实盘监控（:00 整点，4 Phase：采集→验证→持仓诊断三级沉默→北向预警）
  2. `automation-1784039339114` 📈【盘中】投顾策略执行（:10 触发，比助理晚10分钟，4 Phase：采集→信号判定→执行→推送）
- **关键改进**：
  - 移除 buggy 时段门禁（`H=$(date +%H%M)...`），rrule + check_trading_day + check_schedule 已足够
  - 整点/:10 触发代替 :30，避免排队延迟误杀
  - 资产隔离：助理操作 $USER_DATA，投顾操作 $SIM_DATA
  - 助理无交易权限，投顾全权自主（sim_trade.py）
- **待确认**：旧自动化 `1783310235388` 应 PAUSE（新链上线后功能完全覆盖，保留旧链会产生重复推送）

## 🔧 fetch_holdings_quotes.py 重建（22:05）

## 🔧【治理】cost_monitor.py 三副本清理 + 自动化路径修复（22:02）
- **背景**：双副本审计发现 `cost_monitor.py` 存三副本...
- **结果**：全仓仅留 1 个 cost_monitor.py（canonical），$SCRIPTS/cost_monitor.py 现在会 FileNotFound 而非静默空跑

## 🔧 fetch_holdings_quotes.py 重建（22:05）
- 技术债条目 → 已解决。新建 `.workbuddy/scripts/fetch_holdings_quotes.py` (162L)
- 功能：读 portfolio.json → 腾讯 qt.gtimg.cn 批量拉取 → 输出 JSON（现价/涨跌幅/盈亏/成交额/换手率）
- 验证：exit 0，三只持仓全部拉取成功（中天科技 ¥42.85 +3.7%、有研新材 ¥52.63 -2.5%、风华高科 ¥59.41 涨停）
- 下一交易日「综合持仓监控」Step 3 首次正常执行
- MEMORY.md 技术债 ✅ 已标记已解决

## 🔧【治理】cost_monitor.py 三副本清理 + 自动化路径修复（22:02）
- **背景**：双副本审计发现 `cost_monitor.py` 存三副本（canonical `scripts/cost_monitor.py`(113L orchestrator) + deprecated `.workbuddy/scripts/cost_monitor.py`(273L) + legacy 副本(274L 仅头部差异)）
- **关键发现**：`automation_preamble.sh` 中 `$SCRIPTS` = `.workbuddy/scripts`（设计决策，非 bug），导致 `automation-1782002819199` 的 `$PYTHON $SCRIPTS/cost_monitor.py summary` 实际指向废弃副本。废弃副本不支持 `summary` 子命令（仅 `daily`/`check_budget_status`），此调用一直静默空跑
- **修复**：
  1. `automation-1782002819199` prompt 改 `$PYTHON $SCRIPTS/cost_monitor.py summary` → `cd $CLAW && $PYTHON scripts/cost_monitor.py summary`，后续会正确调用 canonical `budget_guard.py status`
  2. 按铁律删除废弃文件（先 cp 到 `/tmp/cost_monitor_deprecated_bak_20260714/` 验证哈希一致 → 删源）
  3. 审计引用的两个"幽灵自动化"(1781428691860/1782002178528)在注册表中不存在，无需处理
- **结果**：全仓仅留 1 个 cost_monitor.py（canonical），$SCRIPTS/cost_monitor.py 现在会 FileNotFound 而非静默空跑

## 用户追问：安全版结构 + OCR 失败根因排查（10:40）

### 安全版结构
- 名称：`📊【炒股助理】公众号投资早报（安全版）`（ID `automation-1780651521899`）
- 性质：独立自动化，覆盖 5 个公众号信号采集（投资明见/恩哥箴言/丹木说/好运侠客/猫笔叨）
- 状态：**已于 2026-07-14 彻底下线**（用户确认：付费 RSS 覆盖账号更多，无需保留）
- 清理范围：DB 软删除记录 + inventory 第 67-68 行旧条目 + MEMORY.md 废弃清单引用，均已移除/标注
- 与当前早报关系：数据由主早报 `1782741941693` 通过付费 RSS（wx_rss_auth）直接采集，覆盖更广

### OCR 失败根因
- **之前成功**（自动化 `1782886699248`「鱼盆模型板块轮动数据抓取」，删于 2026-07-07）：
  - 方案：自动化 prompt 让 LLM 用 `Read` 工具**直接看图片**，由模型视觉理解表格结构 → 成功转录
  - 依赖 `scripts/fetch_yupen_images.py --days 2`（已丢失），最后成功运行 2026-07-06 15:19
- **现在失败**（我建的 `1783472286775` + `fetch_yupen_rss.py`）：
  - 方案：用 `tesseract` 脚本自动 OCR 中文表格 → 行结构断裂，无法解析
  - 本质差异：**LLM 视觉看表 >> OCR 引擎解析中文表格**
- 结论：OCR 引擎（tesseract/chi_sim）不适合鱼盆这种密集中文数字表格；应改回 LLM 视觉方案或换 PaddleOCR/easyocr。

### 后续选项（待用户确认）
1. 鱼盆自动化 prompt 加「用 Read 工具看 `yupen_*.png` 并写 JSON」步骤（恢复之前成功路径）
2. 换 PaddleOCR/easyocr 专门中文 OCR
3. 现状维持（手动读图转录，数据已更新到 7/10）

## 鱼盆 OCR 修复最终落地（11:00 前）

### 方案1 已采纳：LLM 视觉 OCR（恢复之前成功路径）
- 自动化 `1783472286775`（🐟 鱼盆数据提取）升级到 **v4**
- 第 2 步从「tesseract 脚本 OCR」改为「**LLM 用 Read 工具直接看图片 → 写 JSON**」
- 模型：用户纠正 → **`deepseek-v4-flash`**（积分轨 + 支持视觉，非 glm-5.0-turbo）
  - ⚠️ 关键认知：deepseek-v4-flash 走积分轨且有视觉能力，之前误判为无视觉才切 glm，已改回
- 工作流程：
  1. `fetch_yupen_rss.py` 直连付费 RSS 抓图 → `output/yupen/yupen_<date>_*.png` + `yupen_<date>_raw.json`
  2. 自动化模型（deepseek-v4-flash）用 Read 工具看图片 → 视觉理解表格 → 写 `yupen_<数据日期>_sector_rotation.json` + `yupen_<数据日期>_yupen_trend.json`
  3. `read_yupen_data.py` 读最新数据 → 早报引用
- 当前有效数据：`yupen_2026-07-10_sector_rotation.json`（14板块）+ `yupen_2026-07-10_yupen_trend.json`（20指数）已就绪
- `transcribe_yupen_image.py`（tesseract方案）保留备用，但 prompt 已不调用

## 安全版清理最终落地（选项B，11:10）
- 用户确认：付费 RSS 覆盖账号比安全版多，**可删**
- 清理范围（全部完成）：
  1. DB：`automation-1780651521899` 早已软删除（2026-06-21），用 automation_update 删会报 not found，未物理抹除（保留更安全）
  2. `automation-inventory.md` 第 67-68 行（安全版+微信读书早报旧条目）→ 替换为清理说明「功能由主早报付费RSS采集替代」
  3. `MEMORY.md` 废弃版本清单：移除 `1780651521899`，标注「已于 2026-07-14 彻底下线」
  4. `2026-07-13.md` / `2026-07-14.md`：相关描述更新为「已下线/已清理」
- 验证：当前早报 `1782741941693` 不引用安全版 ✓；无任何活跃自动化依赖它 ✓

## 端到端测试 v4 全流程（11:25-11:30）
- 手动触发 `fetch_yupen_rss.py`：成功，选定文章 `[猫笔叨的读后感专区] 被蛇吓到了，胆小勿入…`（评分155），下载 4 张图片，写 `yupen_2026-07-14_raw.json`（status=pending_ocr）
- 模拟 LLM 视觉 OCR：用 Read 工具识别图片 → image_1=20指数趋势表，image_2=14板块轮动表，image_3=无关市场截图，image_0=文章头图
- 已写结构化 JSON：
  - `output/yupen/yupen_2026-07-07_sector_rotation.json`（14板块，数据日期2026.07.07）
  - `output/yupen/yupen_2026-07-07_yupen_trend.json`（20指数，数据日期2026.07.07）
- `raw.json` 状态已更新为 `done` 并记录 `output_json`
- `read_yupen_data.py` 验证：status=ok, 14板块+20指数，当前最新数据仍为 **2026-07-10**（因为 7/10 数据比 7/7 新）
- freshness=stale 属正常（公众号最新数据就是 7/10）
- 原 `/tmp/claw_lock_鱼盆数据提取_20260714` 锁已删除，让 **14:00 自动化自然跑一次**，作为 v4 prompt 自动化模式的真实验证
- 推送步骤：因 freshness≠today，按 prompt 不会触发飞书推送

### 待验证
- 14:00 自动化运行时，deepseek-v4-flash 是否能按 v4 prompt 自动调用 Read 工具看图并正确写出 JSON（本次为人工模拟）
- 若自动化成功，今后将无需人工转录；若失败，需再调整 prompt 或加显式工具调用指引

## 用户追问：今天发了鱼盆表为什么没抓到（11:40）

### 根因
1. **RSS 同步延迟**：今天 7/14 08:20 发的鱼盆表在 `猫笔叨的读后感专区` 文章「心痛，狗东西就是狗东西…」里，但 RSS 后端同步有延迟，9:00 自动化和 11:30 第一次手动跑时还没出现在 `fetch_all_articles` 返回中。
2. **详情抓取抖动**：脚本并发抓详情时，7/14 最新文章详情可能超时/失败，body 为空 → 评分 0，于是被旧数据（7/8 的「被蛇吓到了」）反超。
3. **没有最新文章保底**：脚本只按最高分选，没处理「冠军不是最新文章」的情况，导致即使当天有新帖也漏掉。

### 修复（fetch_yupen_rss.py 已更新）
1. `_get_article_detail` 重试从 4 次 → **8 次**，应对 RSS 后端抖动。
2. 改为「**优先取最新文章 + 评分验证**」逻辑：每个账号只取最新文章，评分仅用于验证该文是否含鱼盆表，避免旧数据反超。
3. 结果：正确选中 `心痛，狗东西就是狗东西…`（评分 **182**），下载 4 图。

### 用户纠正（2026-07-14 13:xx）
- 用户纠正：「他每天都会发鱼盆，所以不存在你说的关键词检索不存在」。
- 之前日志/记忆中「鱼盆数据帖非每日更新，约 2~3 天一篇」描述错误，已更正为「每天发布」。
- 脚本逻辑已同步简化为：优先取各账号最新文章 + 评分验证。

### 验证结果（11:55）
- `fetch_yupen_rss.py` 重新运行：正确选中 `心痛，狗东西就是狗东西…`（评分182）
- 模拟 LLM 视觉 OCR：识别数据日期为 **2026.07.13** 的 14 板块 + 20 指数表格
- 已写：
  - `output/yupen/yupen_2026-07-13_sector_rotation.json`（14板块）
  - `output/yupen/yupen_2026-07-13_yupen_trend.json`（20指数）
- `read_yupen_data.py`：status=ok, data_date=2026-07-13, freshness=stale（数据日期是 7/13，文章是 7/14，脚本语义正确）
- 当前最新有效数据从 7/10 刷新到 **7/13**。

## ⚠️ 防回退锁定（本次操作结论，勿再改）
- 鱼盆自动化 `1783472286775` 模型必须为 **deepseek-v4-flash**（视觉+积分），禁止改回 glm 或其他
- 鱼盆 OCR 必须用 **LLM Read 看图方案**，禁止再切回 tesseract 脚本自动 OCR（中文表格质量差）
- `1780651521899` 公众号投资早报安全版已彻底下线，禁止恢复或重建同名自动化
- 当前早报 `1782741941693` 是唯一推送早报的自动化；鱼盆提取自动化只更新数据文件不推送（避免飞书推两遍）
- 鱼盆数据帖每天发布，数据日期通常滞后文章发布日期1天（如 7/14 文章含 7/13 数据）；`freshness=stale` 多为数据表日期滞后，非 bug
- **fetch_yupen_rss.py 必须保留「优先取最新文章 + 评分验证」逻辑**，禁止简化为只按最高分选（会漏抓当天新帖）

## 📈【投顾操盘】午间选股 v5 (12:30)
- 基建：交易日✅；schedule_utils check✅；`fetch_midday_data.py` 仍缺失 → 沿用 `qt.gtimg.cn` 实时全量扫描(30只池,12:05快照)；`midday_data.json` 仍07-10[STALE]未采用；`strategy_library.json` 不存在(以stock_pool.json为准)。
- 行情：温和risk-off震荡，上证-0.66%/深证-0.42%/创业-0.29%，池内14涨16跌；领涨被动元件+3.67%/AI算力+3.31%(沪电+7%)/消费+1.54%。非07-13极端。
- 早盘复盘：方案A双环+富联🔴放弃(原低分4.40无改善)；方案B安井(603345)🔴放弃(+1.61%@85.21未回踩80-82且100股超现金下限)；方案C全现金🟢继续。
- 午间扫描：⭐新增中国巨石(600176)¥50.5-52.5(错杀反弹+2.83%,7家券商金股,预算内,顺周期集中度32.9%)；🟡备选亨通光电(600487)¥69.5-72.0。
- 现有3仓全部浮盈远离止损(美的+3.6%/长电+2.8%/紫金+0.16%)，无高危仓。
- 输出：`experiments/midday_pick_2026-07-14.json` + `/tmp/midday_plan.json`(中国巨石active/安井conditional)；飞书推送✅；schedule done✅；cost_tracker无配置跳过。

## 鱼盆 v4 全流程收尾（13:39-14:00）
- 简化版 `fetch_yupen_rss.py` 重新验证：正确选中 7/14 文章 `心痛，狗东西就是狗东西…`，下载 4 图，raw 状态 `pending_ocr`。
- 人工视觉转录：14 板块轮动表 + 20 指数趋势表（数据日期均为 2026.07.13），覆盖写入 JSON。
- `read_yupen_data.py` 验证：status=ok，data_date=2026-07-13，stale_note 已改为「数据日期滞后于文章发布日期（通常滞后1天）」。
- 修正记忆/日志错误描述：将「鱼盆数据帖非每日更新，约 2~3 天一篇」改为「每天发布，数据日期通常滞后1天」。
- 更新文件：`.workbuddy/memory/MEMORY.md`、`overview.md`、两个 automation memory 文件。

## 投顾操盘取消 3 只持仓限制（13:50）
- 用户指令：取消投顾模拟盘「同时≤3只」限制。
- 改动：`.workbuddy/data/simulation/STRATEGY.md` 仓位管理表「同时最大持仓数」改为「不限制（AI 自主决定）」；买入前检查清单移除「持仓数 < 3 只」项；`MEMORY.md` 同步更新。
- 确认：`sim_trade.py` 代码中无持仓数量硬限制（仅校验单只≤50%与受限板块），故无需改脚本。
- 影响：投顾 AI 后续可自由加仓至 >3 只标的，仍须遵守单只≤50%、现金≥¥3,000、禁创业板/科创板/北交所/ST。
- 待 14:00 自动化批次验证 deepseek-v4-flash 自动视觉 OCR。


## 投顾操盘策略升级 v2.0（13:57）
- 用户指令：总资金放宽到 ¥50,000、取消现金保留限制、选股规则可调用其他项目成熟规则、其余自行优化。
- 改动清单：
  1. `sim_trade.py`：`INITIAL_CAPITAL` 30000 → **50000**（编译✅）。
  2. `portfolio.json`：`config.initial_capital` 30000 → 50000（已实盘状态，不动现金余额，仅更新配置值）。
  3. `STRATEGY.md` 重写为 **v2.0**：总资金 ¥50,000；现金保留「已取消」；选股体系升级为**复用 QTS 成熟策略 COMBO = VWM(0.6)+BBR(0.4) 加权投票**为主信号 + ADX/DMI(≥25) 趋势过滤，含具体参数与入场纪律（COMBO=1 且 ADX≥25 建仓，RSI>80 禁追）；止损新增「COMBO 翻 -1 减仓离场」。
  4. `MEMORY.md` 模拟炒股系统段同步更新（总资金/现金取消/COMBO 体系/代码常量）。
- 保留约束：单只≤50%、单行业≤60%、亏损≥8% 无条件止损、禁创业板/科创板/北交所/ST、月度目标≥30% 不变。
- QTS 策略参数来源：`QuantTradingSystem/strategy-service/services/signals.py` + `core/config.py`（MAX_POSITION_RATIO=0.30、STOP_LOSS=0.08、TAKE_PROFIT=0.30 为 QTS 默认值，投顾沿用 8% 止损）。

## 选股规则全面接入 + 实战复盘优化（14:04）
- 用户授权：所有项目与选股相关的规则均可接入使用，仅保留「禁买创业板/科创板/北交所/ST」硬约束，其余由 AI 自行判断是否需要改。
- 盘点范围：Claw(sim_signal_advisor.py/STRATEGY.md/strategy_library.json) + QTS(strategy-service/signals.py/core/config.py) + 实盘表现（本金30000→32122,+7.07%，3笔买入全浮盈，无止损触发）。
- 实战判断结论（需要改的）：
  1. `sim_signal_advisor.py` 实际跑选股的逻辑还停在 v1.0，硬编码 `MAX_POSITIONS=3`/`MIN_CASH_RESERVE=3000`/`MAX_SECTOR_PCT=0.40`/`TOTAL_CAPITAL=30000`/`PORTFOLIO_TARGET=39000`，与 STRATEGY.md v2.0 冲突 → **全部对齐更新**（99/0/0.60/50000/65000）。
  2. 接入 QTS 成熟规则：COMBO 建仓强制 ADX≥25 趋势过滤 + RSI(14)>80 超买拦截（新增 `ADX_TREND_FILTER`/`RSI_OVERBOUGHT_BLOCK` 常量，候选阶段落地）。
  3. `get_portfolio()` 硬编码 30000 回退 → 改为读 TOTAL_CAPITAL 常量。
  4. 可用资金计算去掉 MIN_CASH_RESERVE 扣减（现金保留已取消）。
- 不需要改的：禁买板块（用户唯一保留）、单只≤50%、亏损≥8% 止损、月度≥30% 目标——实战 3 笔全盈利验证有效。
- 验证：py_compile ✅；dry-run 输出 available_slots=96、available_cash_for_buy=13073（全额）、total_asset 真实读取，新规则全部生效。
- 文件：sim_signal_advisor.py（v2.0）、STRATEGY.md（补落地说明）。

## 投顾实时选股跑通（14:15）
- 用户确认"可以"跑一次实时选股。
- 完整链路：westock MCP 拉 K线(120日) → 复用 QTS signals/indicators 算 COMBO(VWM0.6+BBR0.4)+ADX+RSI → 套用 v2.0 规则（ADX≥25 过滤、RSI>80 拦截）。
- 落地脚本：`local_combo_signal.py`（支持 --db 模式：QTS Postgres 历史 + 腾讯实时补最新一根；容器内 PYTHONPATH=/app 运行）。
- 关键障碍与解决：
  1. QTS `live_pipeline.py` 从 `/tmp/claw_data/portfolio.json` 读持仓 → 容器内缺此文件 → docker cp 持仓进容器解决。
  2. 容器内 `daily_quote` 表对 600900/601899 仅 40 行（数据到 6/17）→ 用腾讯实时接口(qt.gtimg.cn)补 7/14 当日一根 → 41 天可算（阈值放宽到 30）。
  3. `calculate_adx` 返回三元组(plus_di,minus_di,adx) → 取[2]修正。
- 扫描结果（2026-07-14 盘中）：
  - 美的集团(000333) 80.86 | COMBO=0 VWM=0 BBR=-1 ADX=22.6 RSI=73.7 → 持有(ADX弱)
  - 长江电力(600900) 28.49 | COMBO=0 VWM=0 BBR=0 ADX=34.6 RSI=62.8 → 持有
  - 紫金矿业(601899) 29.17 | COMBO=0 VWM=0 BBR=0 ADX=33.7 RSI=43.4 → 持有
- 结论：3 只现仓均无买入信号（COMBO 全 0），无新建仓建议。新规则 ADX 过滤正确生效（美的被 ADX<25 拦截）。
- 输出：`output/sim_signal_scan_2026-07-14.txt`

## 投顾扩大池扫描实跑（选2：回填科技+红利，14:30）
- 用户"选2，优选科技和红利板块"落地：westock 批量拉 38 只主板标的 120 日 K 线（3 批，每批 180~212KB 超限落盘，本地解析 nodes[]→紧凑 OHLCV JSON 写 `expanded_pool_klines.json`）。
- 池：科技 14 + 红利 24（银行6/电力4/煤3/油2/家电3/高速2/茅五平紫），全主板、无创业板/科创板/ST。
- 复用 QTS COMBO=VWM(0.6)+BBR(0.4) + ADX≥25 过滤 + RSI>80 拦截（v2.0 规则），容器 `scan_expanded_pool.py` 实跑。
- **结论：全池 COMBO=0，0 只买入候选**。VWM/BBR 全 0/-1（无动量无均值回归，死水震荡）；ADX 中位~18，仅 14/38≥25；浦发 RSI=82.3 超买拦截。现仓美的/长电/紫金 ADX 仅 10~18 趋势极弱。
- 与早报"7/13 系统性回调→风控优先不新建仓"完全自洽，模型验证通过。
- 交付：`output/sim_scan_expanded_2026-07-14.txt`；`scan_expanded_pool.py` + `expanded_pool_klines.json` 已固化，后续可直接 docker cp 重跑。
- 管道经验固化：westock 批量 → 落盘 → 本地解析 → docker cp 进容器跑 QTS 指标，是可复用全市场扫描模式（已写入自动化 memory）。

## 助理全主板选股器落地 (B方案, 15:02-15:30)
- 用户确认助理需主动推荐新股票 → 选 B(全主板选股器)
- 基础设施核查: QTS daily_quote 实际有 **3521只**全市场日线(到7/13), 非之前以为的7只 → 跳过westock回填
- 基准池构建: SQL过滤主板(600/601/603/605/000/001/002/003)+近20日均额≥3亿 → 1084只; AkShare代码名映射剔8只ST/退 → **最终1076只** `mainboard_scan_pool.json`
- 扫描脚本 `scan_mainboard_full.py`: 复用QTS COMBO=VWM(0.6)+BBR(0.4), ADX≥25过滤, RSI>80拦截, COMBO≥0.2买入; 容器内跑
- 实测(7/14): 全主板1067只有效, **0只买入候选**(VWM/BBR全0, 死水期), 与投顾扩大池结论自洽
- 固化规则到 MEMORY.md「助理全主板选股规则」段
- 建收盘自动化 `automation-1784013251841`(交易日15:30跑, 有候选推完整建议/无候选推简短无信号)
- 依赖文件: scan_mainboard_full.py / mainboard_scan_pool.json / astock_code_name.json(ST映射, 5528只)

## 收盘综合复盘+晚报 (automation-1782817769722, 15:35)
- 市场：7/13系统性暴跌后 7/14 全面反弹（上证+1.36%/深+2.77%/创业+3.43%/科创+0.77%），定性为超跌技术修复非反转。
- 实盘3只仍全低于-8%止损线（中天42.85/有研52.63/风华59.41），风华涨停(+10%)但仍-15.25%未脱困 → 三只止损事件未解除，明日开盘须执行。
- 模拟盘防御三持仓逆势盈利（美的+3.12%/长江+3.49%/紫金+5.42%，+9.04%）。
- 鱼盆数据 STALE(2026-07-13)，板块吻合率≈40%（反转日模型失准，与7/13一致）；创新药主线确认，红利实际强但模型标弱。
- 7/14早报卡片已推送（_push_wx_card.py硬编码），动作闭环：风华"反弹¥56减仓"预测✅但操作未落实；中天"持有至7/15除权(10派2.60)"✅成立；7/15除权为明日催化。
- 宏观评分40偏多(LPR[缺失])；融资余额[缺失](akshare接口名不匹配)。
- 推送：飞书云文档 https://tcnuc7gyxhyd.feishu.cn/docx/AFhfdm5AEoYSgCxLTWdcbgS6nYb + 结构化卡片(含风险/信号/板块吻合率/持仓RSI/止损/预案/合规/链接)。
- 报告：output/wx_reports/20260714_evening.md；锁文件已建。

## 🛡️ 自动补跑 automation-1783469126245（16:25）
- 检查结果：✅收盘复盘 `/tmp/claw_closing_report_2026-07-14.txt` ✅早报 `output/wx_reports/20260714_morning.md` ❌持仓监控(0条锁定)
- 补推：持仓快照（实盘三只 + 模拟盘三只收盘价 + 盈亏）已推送飞书群 ✅

## Claw ruff 批量修复 43 项 (18:25-18:55)
- 用户要求"批量修复"工程质量报告的43项待处理
- 根因: ruff.toml 的 per-file-ignores 里 `.workbuddy/scripts/*.py` 缺 S608/SIM117/SIM105/PLW2901/PLW0603 (scripts/*.py 有但.workbuddy/scripts/没有); ruff --fix 对这些被ignore规则不生效
- 手动修复: E701/E702拆行, SIM115改with, PLR0124(v!=v→math.isnan), PLW2901改循环变量名, PLW0603/S314加noqa, SIM117合并with, E402移import, S608改参数化查询
- 最终: ruff check 全量 0 errors; 11文件 py_compile 全过
- 提交 ce7511b (本地main, 未push); 仅提交ruff修复12文件, 未混入其他工作区改动

## 🔧 双副本/软链审计（21:00 自动化，大重构触发）
- 判定：当日变更 **94 文件 ≥10 → 大重构**，跑完整7步审计（非周五但变更量触发）
- 发现：cost_monitor.py 三副本+逻辑漂移
  - ① scripts/cost_monitor.py(113行,sha bfa20eb7) canonical，被 1782002178528/1782002819199 调用
  - ② .workbuddy/scripts/cost_monitor.py(273行,sha 68e23ce1) **DEPRECATED(2026-07-13)**，相似度0.052，仍被 1781428691860【💰AI成本监控】调用
  - ③ .workbuddy/scripts/cost_monitor_legacy.py(274行,sha cd4d3920) 与②亦分歧
- 软链全景0、隐形双副本0、数据层软链正确单源(不报)
- 处置：仅报告不修改 → bot身份推送飞书群(om_x100b6a57663bd4a0b1464bba252af80)，列问题/建议(A重指路径B删废弃副本C先备份)，**等待用户指令**

## 📈【盘中】投顾策略执行 automation-1784039339114 (23:42, 非交易时段)
- 交易日✅ / check_schedule(OPEN) → 执行；23:40 已过 14:55 窗口 → 仅诊断不交易
- 大盘：上证 3967.13(+1.36%) / 深证 14924.87(+2.77%)
- 持仓(07-14真实收盘)：美的81.18(+3.13%)/长江28.55(+3.50%)/紫金29.06(+5.42%)，全浮盈
- 信号：止损(≤-8%)无 / 止盈(≥+30%)无 / COMBO翻-1无(离线无指标) / 仓位合规(单只≤18.4%, 三行业分散)
- 决策：无操作 → SILENT(不推飞书)；done_schedule 置日锁
- 数据修复：portfolio.json current_price 滞后4交易日(停07-10) → 用07-14真实收盘刷新 + 补 daily_snapshot[2026-07-14]；备份 /tmp/portfolio_sim_backup_20260714.json
- ⚠️ 待确认：portfolio.json initial_capital 仍 30000，与7/14"总资金放宽¥50,000"(sim_trade.py INITIAL_CAPITAL=50000)不一致；累计收益暂按数据实际基线30,000记(¥32,713,+9.04%)。影响「单只≤¥25,000」口径，建议统一。
