# 2026-08-02（周日）记忆+上下文维护（automation-1780769419635, weekly+daily）

原始指令：🧹【系统维护】记忆+上下文维护 v8 — 合并上下文健康度+周末维护+全局记忆+每月审计（F8安全清理）

## 执行摘要
- 深度: weekly（周日7）+ daily；monthly 跳过（1号已审计）
- 用户级记忆: SOUL 2468B(82%⚠️) / MEMORY **2875B(🔴>2KB, 持续第4日, 与7-31/8-01持平, 跨项目未擅改)** / USER 914B(89%⚠️) / IDENTITY 400B(✅)
- 项目记忆: MEMORY.md 健康；日日志 16个；自动化memory 全≤3000B ✅
- 7天日志: 仅缺今日（本运行补建）
- PHASE2: 空文件0 / 磁盘32% / Docker 0%可回收（无prune）/ 自动化健康 55个 🟢50🟡5🔴0 (90.9%) / brv 5文件
- 修复: 无主动修复；结果 **[SILENT]**（慢性🔴无新增恶化，维持历史决议）

## 公众号正文回填（automation-1785506323216, 08:06）
- 执行：`backfill_wx_content.py --max 40 --gap 2.0`，耗时3m19s
- 结果：ok=3 / fail=14 / remaining=470（473-3 相符）；第17篇后连续安全验证提前收工（正常保护）
- 成功：好运侠客×3（553/772/647字）；持续失败：板神猫哥/草原猎手景月/股海孙大圣/飞龙山侠/天财龙哥（疑文章已删）
- 推送：remaining≠0 → 静默未推送

## 信号溯源 v9.0（automation-1780964240621, 08:08-08:35）
原始指令：📊信号溯源+升降级+CSV+QTS共识 | v9.0 — 新增 QTS双源共识+市场状态

- 全链跑通：COMBO无新信号 / QTS隔离 / regime震荡0.5x / verify 290条216验证(74.5%) / weights 17号 / consensus 148配对 / CSV已生成(data/signal_audit_2026-08-02.csv)
- 总体：胜率38.2%→40.2%，均收益-9.92%→-8.88%，信号277→290
- 唯一评级变化：好运侠客 32%→48%（⚠️监控→✅正常）— **审计后判定不成立，维持监控**
- 推送2条：主报告 om_x100b69dcbc5d1ca0b1d3d62f9b8057a + 数据质量修正 om_x100b69dcb3a5b0acb1f4fcdb68722c3
- 完整报告：output/signal_trace_2026-08-02.md

### 🔴 P0 数据质量根因（新发现，待修）
1. **信号库15条完全重复**（去重键=账号+股票+日期+方向），好运侠客独占6条 → 命中率虚高
2. **日期三格式混用**：ISO / 中文"2026年6月1日"(31条) / RFC822"Sat, 27 Jul" → 后两者 parse_date 失败，不进30日滚动窗口 → 回退累计口径 → **这是非交易日命中率跳变(+16pt、同25样本 hits 8→12)的直接根因**
3. **单票主导**：好运侠客25样本中13个是002354天娱数科(52%)，独立标的仅11只；另有 stock_code="未明确" 脏记录

### 🟡 P1 其他异常
- QTS `[QUARANTINE] WF验证0/10通过` → v9.0核心增量「双源共识」本轮实际未生效（双源0/强共识0/分歧0）
- `qts_regime.json` data_points=0，「震荡0.5x」是兜底默认值非实测 → 早/晚报直接引用会误导仓位
- `python3 -m claw.monitoring.signal_verify` 写完报告(08:19)后进程挂起(S态0%CPU，10min+不退出)，疑非守护线程阻塞解释器退出 → 前台超时被迫后台化
- `signal_verify_history.json` 只有2026-07-12一个快照，历史未累积，升降级只能比对隔日CSV
- 收益率量级异常（-45%~-50%多个账号），疑含退市/重组/未复权标的

## [13:00槽] 助理实盘监控 — 非交易日 SILENT + 调度器 BYDAY 失效查实
**原始指令**：📊【盘中】助理实盘监控 v1（自动化 1785123941709 定时触发）
- 周日休市 → check_trading_day NO → exit 0，无采集无推送
- 🔴 **查实系统性缺陷**：WorkBuddy 调度器忽略 rrule 的 BYDAY 字段，工作日限定任务按 FREQ=DAILY 在周末照跑。全库 35 条工作日限定 ACTIVE 自动化中 8 条最近一次落在周末；且触发时间系统性提前 3~8 分钟（13:00槽实际 12:55）
- 受影响：助理实盘监控 5 槽 / 国金账户数据同步 1785421192240（08-02 Sun 11:31 已跑）/ 盘后账户汇总 1785421201464 / 国金尾盘同步 1785565841913
- 结论：监控类有交易日闸门兜底无害；**账户同步/汇总类须补 check_trading_day 闸门**，否则周末可能写脏数据。未改配置，待用户确认

## 13:50 📦 Dependabot 日清（自动化 1781871850433）
**原始指令**：📦【系统维护】Dependabot 日清 | v5 — 卡片化推送 + 冲突延迟复查
- 扫描：MarvisBridge []、StockInsight []；QuantTradingSystem 2 个 PR（#24 gitleaks-action 2→3、#25 setup-node 4→7）
- 两 PR 均 MERGEABLE 但 UNSTABLE（5 项 CI 红），核验后确认**全部为 main 存量失败**（HEAD 3c565e8 于 07-31 即红）：ruff 未锁版本漂移 0.16.1（lint/pre-commit/quality-gate）+ mypy 23 errors + build-check exit 127（csso/terser 不在 PATH）
- 🔑 **A/B 对照法（新方法论）**：#24 未动 setup-node 却出现同一 build-check exit 127 → 用「未改该配置的 PR」作对照组反证失败非新引入，比逐条读日志快
- 反 vacuous-green 核验通过：gitleaks@v3 实跑输出「no leaks found」；setup-node@v7 实拉 Node 20.20.2 并消除 v4 deprecation 警告。缺口：#25 同改的 dashboard-build.yml 因 paths 过滤未触发（低风险）
- 操作：squash 合并 #24 #25，三仓库开放 PR 归零；卡片推送飞书 om_x100b69d9f6c974a4b489e15787c1f85
- ⚠️ 遗留：QTS main 长期红灯使「CI 全绿」判据失效，每轮需人工比对基线。建议修 3 项存量债（锁 ruff 版本 / 修 build-check PATH / 清 mypy errors）

## 14:01 💰 成本监控 s2槽（automation-1782002819199）— 全绿 [SILENT] + 新成本漏点
- 数据：今日 ¥12.70(19次) | 本月 ¥44.26/¥400(11.1%，余¥355.74) | 单小时峰值 ¥12.23@08时
- 四阈值全未破（单小时<¥20 / 今日<¥25 / 月<¥400 / <¥450）→ 未推送。reasoner 月占 65.1%>50% 但沿用「结构性高单价非频率失控」判例不告警
- 🔴 **新发现 + 根因闭环：非交易日深度任务空跑（约 ¥80-86/月）**
  - 现象：8/1(六)、8/2(日) 连续两天 `公众号信号溯源`(deepseek-reasoner **¥9.60/次**) 照常全额执行，非交易日无新信号可溯源 → 纯浪费
  - **根因 = 本文件 13:00 槽查实的同一缺陷**：调度器忽略 rrule `BYDAY`，工作日限定任务按 FREQ=DAILY 周末照跑（信号溯源 1780964240621 今日 08:08 即此列）
  - 该缺陷此前只按「脏数据风险」评估（账户同步类），**成本维度是本槽新增的第二重损失**，且 reasoner 是全库最贵单次任务 → 成本影响远大于其他受影响任务
  - 修法：任务前置 `is_trading_day.py` 门禁跳过。**不降模型质量、零副作用**，优先级高于此前 18 轮未获同意的「降模型」方案
  - ⚠️ **待用户确认后再改，未自动执行**（铁律：禁自动降级/改配置）

## 14:05 知识库挖掘 v9（自动化 1782137216020，第84次）
**原始指令**: 定时自动化 — LLM 逐篇精读公众号增量文章，提炼精髓+沉淀信号（周日，跳过周六发现步骤）

- 精读 14 篇 → actionable 2 / 新信号 7；insight 523→552，信号池 290→297
- 观点: 猫笔刀（白酒底仓 600519/000568 + 长鑫存储未上市剔除）；天财龙哥（AI 未终结、光通信反攻 002384/002281/002353/002558/601995）。7 只全主板合规
- 已推飞书 om_x100b69d9ad0500a4b03e36396ea6202；报告 output/gzh_mining_20260802_1405.md
- 🔴 **口径纠正（长效）**: 公众号文章空正文率统计须用 `content_text OR content` **双字段合并口径**。此前只看 content_text 得出 82.5% 属误报，真实为 224/641=34.9%
- 📌 路径: insight/signal 库在 `.workbuddy/data/`（非 `data/`），文章原文在 `output/wx_articles/`

## 14:30 🔧 QTS CI 存量债根因排查 + 修复（承接 13:50 Dependabot 日清遗留）
**原始指令**：用户粘贴 Dependabot 日清报告，落地其「3 项建议修复」

- 🔴 **推翻日清报告的根因判断（A/B 实验证伪）**：报告称 lint 红是「CI ruff 未锁版本漂移 0.16.1 扫出存量代码」。实测 ruff 0.11.5(pre-commit锁定版)=227 errors vs 0.15.17=225 errors → **版本漂移只解释 2 条，锁版本根本不能让 lint 变绿**。真实原因=代码存量 225 条违规(F401×111 + I001×101)，与版本无关。教训：「版本漂移」是极易误导的假根因，须用旧版本实跑对照证伪
- 🔴 **build-check exit 127 是三层缺陷叠加**（报告只猜到「PATH 未配」一层）：
  1. `dashboard/build.sh` 硬编码 `/Users/guan/.workbuddy/binaries/node/.../csso|terser` 绝对路径 → CI 必 127，且泄漏本机用户名
  2. `test.yml` 装的是 `csso`（库包无 CLI），正确是 `csso-cli` → 即使修了 PATH 仍会 127
  3. **自我延续死循环**：build.sh 产物无 EOF 换行，pre-commit `end-of-file-fixer` 提交时补上 → CI 重建必 diff；`git diff --ignore-all-space` **并不能**忽略「缺失末尾换行」（原注释误以为可以）→ build-check 永远红
- 🔴 **真 bug（非 lint 噪音）**：`report_scheduler.py` 用 `logging.getLogger` 却发 structlog 风格 kwargs → INFO 级 `TypeError: Logger._log() got an unexpected keyword argument`，被外层 except 吞掉 → **数据质量检查任务每天两次都误报「失败」**（实际已跑完）。仓库本就有 `shared.logging_config.get_logger` 封装，该模块漏用
- 🔴 `dashboard/index.html` 源文件 meta 标签缺引号：`name="color-scheme content="light dark"` → color-scheme 声明失效（committed dist 反而是对的，说明有人手改过 dist 未回源码）
- ⚠️ 报告漏报：`unit-test(shared)` 实际也红（8 failed/35 passed，既有），测试直接 `_StructuredLogger("x")` 构造未挂日志树 → caplog 抓不到；另 `test_default_unknown` 有测试间污染。未修，待定
- ✅ 已修 8 文件（全部本地实测验证，无回归；日志测试前后对照均 8failed/35passed 完全一致）：test.yml(锁RUFF_VERSION=0.11.5 + csso-cli) / build.sh(resolve_tool PATH→npx回退 + 补EOF换行 + 去掉吞错的2>/dev/null) / report_scheduler.py / logging_config.py(降级路径 setLoggerClass 兜底) / index.html / dist×3
- 📌 验证结论：build.sh 本地实跑 exit 0 且**幂等**；dist 同步校验从 5 文件差异降到 2（均为待提交的源码真实变更）
- ⏸ 未动待确认：ruff 存量 225 条（autofix 覆盖 119 文件/213 条，余 15 条手工含 1 条 S608 SQL 注入）；mypy 22 条（17 条集中在 report_service.py，主因变量名 `e` 与 except 绑定冲突，纯改名即可）；**未 commit 未 push**

## 15:00 📊 信号溯源+升降级+CSV（自动化 1785284629106）
**原始指令**：📊信号溯源+升降级+CSV+QTS共识 | v9.0

- COMBO: 无新信号 | QTS: 10/0 WF通过（连续第6天隔离[QUARANTINE]）| 市场: 🟡震荡(0.5x)
- 信源权重: 17号更新，高信源(城市金融报100%/证券时报100%*1/上海证券报64.3%/猫笔刀60%/第一财经60%)
- 双源共识: 0条（QTS 0条WF通过），149配对/10仅公众号
- signal_verify: Wind日限超时90s kill（连续第6天，周日非交易日正常）
- 推送决策: SILENT（无变化/周日非交易日）

## 15:1x 🔧 QTS CI 收尾（跨项目，QuantTradingSystem）
**原始指令**：续前会话 — 修 Quality Gate 永久红灯 + 验证 CI

- 根因：Quality Gate 走 `engineering-audit-kit@v1` → `ruff check .`（unpinned 最新 ruff），读本仓 `ruff.toml`；新版 ruff 把 PLR0917(参数过多) 晋升 stable → 批量报 29 处（本地锁 0.15.17 不触发）+ 2 处 PLC0207。
- 修复（commit 4bb32b0a pushed to main）：
  - `ruff.toml` ignore 加 `PLR0917`（与既有 PLR0912/0913/0915 同族）
  - 2 处 PLC0207 用 `split(".", maxsplit=1)` 修正（scripts/fetch_kline_tencent.py / strategy-service/services/multi_agent/agents.py），语义不变
- **验证**：CI - Quality Gate = ✅ SUCCESS（31→0）；pre-commit 全绿；本地 `ruff check .` All checks passed。
- ⚠️ **新发现（超出原 5-red 范围）**：Test & Lint 的 unit-test job 仍 RED，全矩阵聚合 **86 处失败**（跨 4 服务）；本地复现相同错误，根因在 `shared/` 日志/中间件基座（service_name_var 默认值 'my-service'≠'unknown'、caplog 未捕获、header 清洗）。**非本次改动引入**（仅动 ruff.toml+2处maxsplit）。属 pre-existing 系统性单测崩坏，需独立排障会话（区分 stale test vs 真回归）。Security Scan 本次仍 in_progress（前次 green，本次改动无安全影响，预期绿）。
- 待用户确认：是否展开单测套件修复（大工程，含判断 stale/回归）。

## 16:00 📈 投顾绩效周报（automation-1785336744681）
**原始指令**：📈 每周绩效跟踪周报 — 本周操作总结 + 辩论统计

- 总资产 ¥54,260.24 (+8.52% 累计)，持仓 3 只全正收益
- 本周操作：卖出长江电力(600900) 200股 @¥28.80，¥5,749.24
- 辩论：近两周 42 次，全持仓一致 HOLD（置信度 47-49%）；华天科技(002185) 曾触发 SELL
- 飞书推送：om_x100b69dbcf83b4a8b4b8d7be638bdb6（卡片）

## 16:18 🔧 F8 交易日门禁修复（手动，用户授权"按你的计划来"）
**原始指令**：用户"按你的计划来" → 落地今天 13:00/14:01 槽论证的「交易日门禁修复计划」
- 根因（13:00槽已查实）：调度器忽略 rrule BYDAY，工作日限定任务周末照跑
- 修复：给 5 个缺门禁任务 prompt 开头加 `is_trading_day.py` 门禁（`exit≠0 → [SILENT] exit 0`）
  - 账户同步/脏数据风险(3)：1785421192240(午间)/1785421201464(盘后汇总)/1785565841913(尾盘)
  - 信号溯源/高成本空跑(2)：1780964240621(05:00,reasoner¥9.6/次)/1785284629106(15:00)
- 验证：`is_trading_day.py` 周日 exit=1(触发跳过)/周一·周二 exit=0(正常) ✅
- 副作用：零（仅周末跳过，工作日照常）；不改模型质量；优于此前18轮未实施的降模型方案
- 范围克制：未扩到 RSS同步/选股池补全/QTS回填/鱼盆等「周末空跑但低成本/幂等无害」类
- 待观察：下个周一(08-03)验证 5 任务工作日正常触发；周末(08-08六/08-09日)确认静默

## 17:0x 🔧 QTS 单测套件修复专项收官（承接 15:1x 的 86 失败）
**原始指令**：用户授权"这个做完" → 修复 4 服务 86 处单测失败（区分 stale test vs 真回归）

- **Task#10 shared/ 基座**：281 passed（real bug vs stale test 判定，基座日志/中间件/ws/异常/redis 对齐）
- **Task#11 ai-scheduler**：22 failed→253 passed。✅ **发现并修复真 bug**：`api/schedule.py` 同秒并发创建任务 task_id 碰撞（秒级时间戳）→ 新增 `_new_task_id` 后缀去重 + `_spawn_background` 抽离便于测试。测试侧 fixture 注入 `MagicMock` scheduler + no-op spawn（TestClient 不初始化 lifespan）
- **Task#11 strategy-service**：34 failed→1246 passed(3 skip)。对齐 source 语义：
  - auth 中间件 `dependency_overrides`(18×401) / 参数化 SQL `:level/:status`+params(3) / `misfire_grace_time=600` / `sync_daily_data` 经 `asyncio.to_thread` 同步调用(非 await) / `report_monthly day="last"` / 信号汇总 kline 三列查询 / ReportService `DEFAULT_STRATEGIES=11`+`MIN_SAMPLES=60` / 8 个 cron job
- **Task#12 全矩阵 + 提交**：shared 281 / ai-scheduler 253 / strategy-service 1246(3 skip) / execution-service 455 全绿
  - commit `5dde67e9` pushed to main（20 文件 +334/−121）
  - 排除 `strategy-service/services/.cache/*.json`（跑测试再生成的产物，`.gitignore` 已含 `**/.cache/`）
  - 根目录 `tests/` 为 E2E（需 Docker 栈 + requests），不在本专项，CI 不跑
- ✅ **专项闭环**：原 Dependabot 报告声称的 5 项 CI 红已全绿，单测套件崩坏(86)系统性修复并合入 main

## 18:2x 🔧 QTS CI 全链路独立复验（收官验证）
**触发**：用户"继续" → 把 13:50/14:30 槽遗留的 CI 存量债一并验完
- 实测当前态（非读日志）：mypy 4 包 **0 errors** / ruff(local 0.15.17 + CI unpinned) **0 violations** / unit-test 全绿且 coverage 达标（shared≥30 / ai 98.25% / strategy 91.86% / execution 86.49%，均 ≥65）/ contract-test **12 passed** / build.sh **exit 0** 且 `git diff --ignore-all-space` 除 build.json(时间戳,设计排除)外无 diff
- CI 配置修复均已合入 main：`589c6b1f`(锁 RUFF_VERSION=0.11.5 + csso-cli + build.sh resolve_tool PATH→npx回退 + EOF换行) / `4bb32b0a`(Quality Gate ruff.toml+PLC0207) / `5dde67e9`(单测修复)
- **结论**：QTS `Test & Lint` 7 job(pre-commit/lint/type-check/unit-test/contract-test/build-check + quality-gate) 全部绿，CI 永久红灯专项 **100% 收官**。剩 working-tree 仅 `build.json`(时间戳)+`.cache`(跑测再生) 两类设计排除产物，不提交
- ⚠️ e2e-test 仍需 Docker Compose 栈（根目录 tests/test_e2e.py），CI 内独立 job，本地不复验（与单测专项无交集）

## 19:4x 🔧 收尾两项（用户"按顺序做"）
**① 取消 .cache 跟踪**：`git rm --cached` 80 个 `strategy-service/services/.cache/*.json`（磁盘保留，仅去索引）。`.gitignore` 已有 `**/.cache/` → 取消后变 ignored，`git status` 归零。commit `f2f90378` pushed。现 working-tree 仅 `build.json`(时间戳)+已 ignore 的 `.cache` 再生产物
**② 根 tests/ E2E 补 requests 依赖**：根因= `dev` extra 仅有 `types-requests` 桩包、缺运行时 `requests` → conftest/test_e2e 集合期 `ModuleNotFoundError`。pyproject `dev` 加 `requests>=2.31.0`（根 tests 未直接 import websocket-client，未加）；venv 装 requests 2.34.2。验证：`pytest tests/ --collect-only` 收 164 个；纯逻辑 test_risk_controller 22passed / test_resilience 21passed。commit `c5e8ad5e` pushed。
- 注：test_e2e.py 真实执行仍需 Docker 活栈（localhost:8000/8001/8002），本改动仅修复导入阻塞，未改 CI e2e job（其已 ad-hoc 装 requests）
