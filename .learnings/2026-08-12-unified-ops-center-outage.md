# ✅已升级(2026-08-16) · 统一巡检中枢整窗失联（~5.3h）

- **发现时间**：2026-08-12 05:01（巡检中枢存活看门狗 automation-1786000625371 每2h 触发）
- **严重度**：高（中枢自身失联，所有依赖它的健康巡检/Docker自愈/CI红扫描在失联期间完全停摆）
- **是否真实告警**：是 — 看门狗历史上首次真实命中（此前 ~70 轮全部 SILENT）

## 证据链（先取证再下结论，禁脑补）
1. 状态锚 `monitoring.global.unified_ops_center.self_health.last_ok_ts = 2026-08-11T23:46:09`（已用 python 直接读 cross_project_state.json 核实，非脚本单侧）。
2. 看门狗算间隔 = 315.5 min > 阈值 180 min → 已推飞书主群告警。
3. 真正写心跳的中枢 = `automation-1785982929477`（🛡️ 统一巡检中枢，每小时，运行 unified_ops_center.py）。看门狗告警文案原误写为 1785506975961（那是 failure-scan watchdog，已修）。
4. 中枢运行时间线：22:47:29(success=1) → **23:44:29(success=0，title=`Run interrupted because the automation orchestrator restarted`)** → 之后 00:47~04:47 共 5 个整点档 **全部未触发** → 静默至 05:03。
5. 失败签名与近期 failure-scan 记录的「会话未拉起（排队/资源紧张）」同源 = orchestrator 重启/硬杀类根因。

## 根因
23:44 中枢运行被 orchestrator 重启打断（success=0），随后调度系统在夜间整窗未再触发该中枢的整点档（疑似 orchestrator 重启后调度回退/资源紧张丢档），导致 last_ok_ts 卡死约 5.3h。

## 处置
- 看门狗按设计正确检测并推飞书告警（独立于中枢的互相 watchdog 韧性结构生效）。
- 调度已自愈：failure-scan watchdog(1785506975961) 于 04:57:38 成功运行(success=1)，中枢应在下一整点档(~05:47)自动恢复；需回读 last_ok_ts 确认。
- 修 watchdog 告警文案错误 ID（1785506975961 → 1785982929477）。

## 待办 / 升级建议
- [x] 次日回读确认 last_ok_ts 已刷新至 ~05:47 后，中枢恢复连续性（08-12 已闭环）。
- [x] 此类「orchestrator 重启打断 + 夜间整窗丢档」已 ≥2 次同类发生 → ✅已升级(2026-08-16)为🔴铁律「双自动化并发操作同一目标须互斥」(互斥/单一配置源/写前2min护栏)。
- [ ] 评估：中枢整窗失联期间，Docker 自愈/CI红扫描/push_feishu 自检等依赖项是否也同步停摆 → 是否需各自独立兜底。

---

# 🔴 复发 · 2026-08-18 第二次真实命中（★同根因二次，强化升级论据）

- **发现时间**：2026-08-18 21:34（巡检中枢存活看门狗 automation-1786000625371 每2h 触发）
- **严重度**：高（同 08-12，中枢整窗失联，依赖它的巡检/Docker自愈/CI红扫描停摆 ~4.8h）
- **是否真实告警**：是 — 看门狗历史第二次真实命中（08-12 首次，本次间隔约 6 天）

## 证据链（先取证再下结论）
1. 状态锚 `last_ok_ts = 2026-08-18T16:43:23`（python 直读 cross_project_state.json 核实）。
2. 看门狗算间隔 = 290.9 min > 阈值 180 min → 推飞书主群告警（管道同 push_card.py，探针验证 rc=0 可达）。
3. `unified_self_heal_log.json` 最后一条 = 16:43:13 → **16:43 后整点档（17/18/19/20/21 点）全部缺失**，与 last_ok_ts 完全吻合 = 中枢脚本未点火。
4. ZHITAI 已挂载（排除 08-17 那种盘未挂载致脚本不可达的脆弱性）。
5. **中枢脚本本身健康**：手动直跑 `unified_ops_center.py` RC=0，15 项检查全过（仅微信公众号 wechat-download-api 登录过期老问题，已被 dedup 跳过推送），成功刷新 last_ok_ts → 21:36:33（间隔 0.3min）。
6. 本看门狗自动化(1786000625371)自身每2h 正常点火 → 编排器非全局死，是**中枢专用调度档被丢/打断**。

## 根因（推断，未硬确认）
与 08-12 同签名：orchestrator 重启/硬杀类打断 + 随后中枢整点档连续 5 档未触发（调度回退/丢档）。
⚠️ 无法访问 automation_runs 日志（automation_update 工具仅 view 定义无 runs 接口），故为推断非石锤；脚本侧已实锤健康，问题锁定在调度层。

## 处置
- 看门狗按设计检测 + 推飞书告警（独立互相 watchdog 韧性生效）。
- **手动直跑中枢恢复心跳**（last_ok_ts 16:43→21:36），监测盲区关闭，下个 2h 周期(~23:34)看门狗将转 SILENT。
- 此操作实测验证了 08-12 遗留「本地 unified_ops_center.py 兜底刷新心跳」fallback **确实有效**。

## 升级建议（闭环 08-12 待办）
- [ ] **固化 fallback 进 watchdog 脚本**：超阈值时除推告警外，自动 `python3 unified_ops_center.py`（或 `--no-push` 仅刷心跳）兜底，消除"告警已发但盲区仍持续数小时"的依赖等待。
- [ ] 反复发（08-12 + 08-18，约 6 天一次）→ 建议将「中枢整窗失联 ≥2 次」升级为独立🔴铁律或并入现有铁律的"调度层须自愈兜底"条款，而非仅靠外部 watchdog 告警。
- [ ] 探究为何中枢专用档被丢而本看门狗档正常 → 需平台侧 run 日志权限，或在中枢自身加"若距上次成功 > X 则自唤重跑"的内置心跳自愈。
