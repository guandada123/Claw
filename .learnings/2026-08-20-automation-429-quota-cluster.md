### 2026-08-20 automation_runs "refusal" 集群实为 429 配额耗尽（correction + insight）

- **类型**: correction（修正初判）/ insight（诊断模式）
- **现象**: 08-19 20:09–21:28 出现 12 条 `Automation prompt stopped before completion: refusal` 集群（投顾策略执行/统一巡检中枢/扫表自身/双副本审计/日报校验/存活看门狗）。watchdog 归"次要" SILENT。初判（含本助手首轮摘要）误写为"模型拒绝执行 / 内容策略异常"。
- **根因**(DB 实锤): 实际是 **HTTP 429 配额/频率限制**——`code: -32003`, `message: "Quota exceeded: 429 您的使用量已超出频率限制，将在 2026-08-20 02:09:25 UTC+8 重置"`。
  - **系统性非单任务**: 6 个互不相干的自动化同享**同一个重置时间戳 `2026-08-20 02:09:25`** → 单一模型路由当日配额被打满，非任一自动化内容触发。
  - **偶发非周期**: 08-16/17/18 的 429 失败均为 0 条；08-20 02:10 重置后至今 0 条 → 08-19 当晚用量 spike，不是每日 recurring 配额耗尽。
  - 与记忆已知待办吻合：「DeepSeek 直连余额已近枯竭 / CatRouter·CrazyRouter 路由切换待完成」。
- **处置**: 无需修复——配额按固定时刻（每日 02:09 UTC+8）重置后自愈，08-20 巡检全绿印证。关联根因仍在 pending：路由切换未完成 → 若后续用量再超阈值会复现。
- **防复犯（诊断纪律落地）**:
  1. `automation_runs.thread_title` 的 "refusal" 字眼 **≠ 内容安全拒绝**，含 429 配额限制。诊断必须取**完整** thread_title 看 `code`/`message`，不能凭 "refusal" 臆断为内容策略。
  2. 判断系统性 vs 单任务：多个互不相干自动化**同享一个 reset 时间戳**同时失败 = 单一路由配额耗尽（系统性）；单任务孤立失败才可能是内容问题。
  3. 确认恢复：查 reset 时间戳之后窗口是否还有新 429；本例 02:10 后 0 条即证恢复。
  4. watchdog `classify()` 把 refusal 归 "other"/次要 → SILENT 行为**正确**（非关键硬杀不打扰）；但其 digest 文案写 "refusal" 易误导人工研判，建议对 `429`/`Quota` 单独标注（可选改进，非必须）。
- **去重**: 与 08-11/08-13 `THINK_BUDGET=high` 注入（thinking 致 content 空）**不同源**——本例是纯配额/频率限制，与 thinking 无关；也与 08-13 Marvis 重启（会话未拉起）不同源。
- **★升级候选**: 若后续 429 复现频率上升（路由切换长期未落地），考虑给 watchdog 增加「429 配额集群」专项检测：窗口内 ≥3 条同 reset 时间戳 429 → 推飞书提示「模型路由配额耗尽，切换模型/检查路由」，避免静默淹没在次要失败里。
