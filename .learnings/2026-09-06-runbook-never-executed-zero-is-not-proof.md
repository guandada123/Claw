# 2026-09-06 · 自愈周报 total=0 的第二层含义：Runbook 从未被执行过（覆盖率盲区）

- **发现场景**：巡检中枢自愈周报（automation-1786000798763）第二次执行，`total=0 success=0 rate=None`。
- **严重度**：P1（可观测性/可信度缺陷，非生产故障）
- **状态**：已取证，待人工决策（是否做一次 Runbook 演练）

## 现象

09-06 周报：近7天 `total=0`（执行类自愈动作 0 条），成功率 N/A。与 08-30 首份周报**数值完全相同**。

## 取证过程（不要只看周报数字）

1. `unified_self_heal_log.json` 当前 121 条 → **全部** `action=detect / result=alert`，0 条执行类。
2. 统计口径确认（`unified_ops_center.py:1893`）：
   ```python
   exec_actions = [e for e in data if e.get("action") not in ("detect",)]
   ```
   即 total=0 是"没有执行动作"，不是"统计脚本坏了"。
3. 查 `.runbook_fuse.json` = `{}` → **无熔断**，所以不是"被熔断挡住"，是**触发条件从未满足**。
4. 回读 09-02 重构前的备份 `unified_self_heal_log.json.bak-20260902`：200 条，**同样 100% 是 detect**。
   → **自审计日志建立以来，从未产生过一条 execute 记录**。
5. 旁路核实：Docker 容器重启走 `self_heal.py`，写的是 `~/.workbuddy/cross_project_state.json`
   的 `monitoring.self_heal_log`，**不进** `unified_self_heal_log.json`。查该键 = **0 条**
   → 容器自愈路径同样近窗口内无执行。

## 结论（这才是最危险的点）

> **"0 失败" ≠ "自愈可靠"，可能只是"自愈从未运行"。**

4 个 Runbook —— `memwatch_threshold_bump` / `docker_restart_container` /
`dependabot_rebase` / `publish_audit_merge` —— 均处于**未验证（unproven）**状态。
中枢依赖它们做自愈，但没有任何证据表明它们在真实故障下能跑通。
一旦真故障发生，可能是"自愈静默失败 + 周报仍显示 0 失败"的双盲。

## 与 08-30 结论的关系（噪音已治理，但暴露了更深一层）

| 项 | 08-30 | 09-06 | 变化 |
|---|---|---|---|
| 审计条目数 | 185 | 121 | ↓35%（09-02 节流改造生效） |
| wechat-download-api 噪音 | 183（98.9%） | 6（5%） | **↓96.7%，P1 已缓解** |
| 主导项 | wechat 单项 | 自动化队列积压 62（51%） | 换了个主导项 |
| 执行类动作 | 0 | 0 | 未变 |

- 08-30 的 P1「审计窗口被单项噪音淹没」**已由 09-02 的同类条目节流修复**。
- 但新主导项「自动化队列积压」根因单一：`concurrency=3` 未限流 → 超发 → 硬超时 CANCELLED
  （与既有铁律一致），**中枢无自愈路径**，只能人工降频 → 属已知未修，不是新问题。

## 待观察/待办

- 🔴 **恶化趋势（新）**：自动化健康 🔴 项数从 09-02 的 8 项单调升到 09-06 的 **40 项**
  （8→12→19→23→27→29→30→…→37→39→40）。近 48 个自动化里 40 个处于红态，值得单独排查。
- 🟡 wechat-download-api 巡检项：告警文案已从「需扫码重登」变为
  「通道早于过期即停摆，重登大概率无法恢复，**建议摘除该巡检项**」→ 建议采纳摘除，
  否则会长期占据审计窗口。
- 🟡 建议：给 4 个 Runbook 各做一次 `--dry-run` 演练（或造一次可控故障），
  在审计日志里留下第一条 execute 记录，把"未验证"变成"已验证"。

## 复用要点

- 判周报 `total=0` 是否是真静默，**三步取证**：①看 action 分布 ②看 `.runbook_fuse.json` 是否熔断
  ③回读 `.bak` 历史判断是否"从来如此"。**只看当周数字会得出"没故障"的错误结论。**
- 跨日志核对：中枢审计日志 ≠ 全量自愈动作，`self_heal.py`（Docker 重启）写在
  `cross_project_state.json`，**必须旁路核查**，否则漏计真实执行。
